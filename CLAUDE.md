# Context for Claude

This file is auto-loaded into every Claude Code session. Goal: don't make a
fresh-context Claude rediscover what M0–M6 already cost time to learn.

## Always read first

Before suggesting architecture, scope, or code structure, read:

1. `PRD.md` — product spec (locked, treat as ground truth)
2. `PHASE1_PLAN.md` — milestone plan + per-M status (kept up to date)
3. `SYSTEM_PROMPT.md` — system prompt construction spec
4. `docs/M0_DECISION_MEMO.md` — ratified hardware/runtime decisions

On demand (don't read cold; read when the work touches that milestone):
- `docs/M{3,4,5,6}_PLAN.md` — per-milestone phase logs + ratified decisions
- `docs/M{3,4,5,6}_*_HANDOFF.md` — operational handoff notes (deferred items, findings)
- `docs/preflight_memory_shared_v1.0.0_MODEL_CARD.md` — classifier eval + known weaknesses
- `CLASSIFIER_DATASETS.md` — pre-flight + memory extractor dataset spec

## Project at a glance

On-device AI assistant for Android. **Pixel 7 + Android 16 only** for Phase 1.

- **LLM runtime:** LiteRT-LM 0.12.0 (`com.google.ai.edge.litertlm:litertlm-android`). Gemma 4 E2B (`litert-community/gemma-4-E2B-it-litert-lm`, 2.58 GB) — text **and image** input (vision; PR #48). GPU via Play Services TFLite OpenCL on Mali-G710; CPU fallback when GPU init throws. E4B ruled out by LMKD thrash on 8 GB Pixel 7. (Bumped from 0.10.2 in PR #48 — 0.10.2 couldn't load the multi-resolution vision encoder; see invariants #39–#40.)
- **Classifier + embedder runtime:** `com.google.ai.edge.litert:litert:2.1.5` (a different runtime from LiteRT-LM; see invariant #18). (Bumped from 2.1.4 in PR #48 so its native libs coexist with LiteRT-LM 0.12.0 — see invariant #40.)
- **Classifier:** shared DistilBERT-base encoder + 3 task heads, INT8 → `models/preflight_memory_shared_v1.0.0_int8.tflite` (67.7 MB), bundled in `:androidApp/src/main/assets/`. Three outputs per forward pass.
- **Embedder:** all-MiniLM-L6-v2 INT8 (23.5 MB), bundled. Mean-pool + L2-norm baked in; output is one 384-dim vector.
- **Architecture:** KMP shared module (`:shared`) + Android Compose shell (`:androidApp`). iOS targets stubbed for Phase 2.
- **Toolchain:** JDK 17, Gradle 9.3.1, AGP 9.1.1, Kotlin 2.3.21, KSP 2.3.7, Hilt 2.59.2.
- **Status:** M0–M6 complete (see `PHASE1_PLAN.md` §5 for per-milestone summaries and test counts). M7 (closed beta → Play Store) not started.

## Hard invariants

Things prior milestones surfaced the hard way — don't rediscover. Numbering is stable; existing code/comments reference these by number.

### Inference runtime (LiteRT-LM, Gemma)

1. **Every LiteRT-LM call must run off the main thread.** `Engine.initialize()` blocks 4–8 s on Pixel 7 and will ANR. `LiteRtInferenceEngine` already wraps in `Dispatchers.IO` — keep it that way for any new caller.
2. **`GenerationRequest.maxTokens` is a no-op in LiteRT-LM 0.10.2.** No per-call cap surfaced. Cancel the Flow at the parser layer to stop early.
3. **`Backend.GPU()` requires `play-services-tflite-gpu:16.4.0` at runtime.** Without it, GPU init throws `Cannot find OpenCL library on this device`. Devices without recent Play Services fall back to CPU via `LiteRtInferenceEngine.tryInitialize`.
4. **Pixel CDEV throttling drops GPU clocks before `PowerManager.currentThermalStatus` reflects it.** If we need accurate throttling signal, infer from measured tok/s drift, not the high-level API.
8. **Tool calling MUST go through LiteRT-LM's structured channel.** Register via `ConversationConfig.tools` (using `tool(OpenApiTool)` to wrap a description JSON). Text-only schemas in the system prompt do NOT unlock tool-use mode — the model defaults to "I don't have real-time data" refusals.
9. **Reuse the same `Conversation` across the multi-step tool-call cycle.** Per https://ai.google.dev/edge/litert-lm/android: `sendMessageAsync(userText)` → if `Message.toolCalls` populated, execute tool → `sendMessageAsync(Message.tool(...))` on the **same conversation**. Re-creating per step and replaying via `initialMessages` does NOT preserve Gemma's call↔response correlation; the model just re-emits the call until the per-turn cap fires. `LiteRtInferenceEngine.generate` drives this loop internally and exposes a `ToolDispatcher` callback to the agent layer.
10. **`Content.ToolResponse(name, response)` needs a structured payload, not a raw JSON string.** Gson treats a `String` as a JSON-quoted string, so passing `"[{\"title\":...}]"` makes Gemma see a quoted blob instead of an array. The engine parses with kotlinx.serialization and hands a `List<Map<String, Any?>>` to `Content.ToolResponse`. See `LiteRtInferenceEngine.parseAsStructured`.
11. **Don't pre-template `<start_of_turn>...<end_of_turn>` markers in the prompt.** LiteRT-LM applies Gemma's chat template internally when you use `ConversationConfig` + `sendMessageAsync`. `PromptAssembler.assembleStructured` returns markers-free.
29. **`Conversation.cancelProcess()` is the only way to actually stop an in-flight decode.** `Job.cancel()` on the consuming coroutine closes the Flow chain but LiteRT-LM's native worker keeps decoding to end-of-turn, holding GPU/CPU and freezing the UI. `LiteRtInferenceEngine.bindCancellation` registers an `invokeOnCompletion` on the current `Job` that fires `conversation.cancelProcess()` when the parent cancels; the finally block calls it defensively before `conversation.close()`. **Don't rely on `Job.cancel()` alone to bound LiteRT-LM CPU/GPU time.** The same primitive exists on `com.google.ai.edge.litertlm.Session` for the lower-level API; not currently wired because production uses `Conversation`. Discovered via `javap` on the api.jar — not currently mentioned in the public LiteRT-LM Android docs. **This cancellation path is the ONLY lever that recovers a GPU-saturation freeze (the M7 main-thread stall), so the watchdog has to drive it, not just the user.** PR #31: `InferenceSessionManager` tracks the in-flight generation `Job`(s) and `forceUnload(UnloadReason.MainThreadWatchdog)` cancels them — which fires `cancelProcess()` via this path — so the deferred unload can actually fire. Before #31 a watchdog trip during a hung decode was a silent no-op: `forceUnload` only set `pendingForceUnload` and waited for the Flow to complete, which never happens while the decode is wedged. **`forceUnload` cancels in-flight generations ONLY for `MainThreadWatchdog`**; `LowMemory`/`TrimMemory`/`Manual` still defer (the app isn't hung in those cases, and cutting a live turn mid-stream is the worse UX). Caveat: if the GPU is wedged so hard that `cancelProcess()` itself can't return, this is best-effort — same limit as the manual Cancel button; the escalate-to-`Process.killProcess` last resort was considered and deferred.

`MarkerFunctionCallParser` is legacy (text-marker workaround pre-#8). With invariants 8–11 in place, tool calls surface structurally via `Message.toolCalls` — the parser is unused in production but still has tests.

### Classifier + embedder runtime (ai-edge-litert)

12. **The classifier .tflite has 2 inputs + 3 outputs identified by `tensor.name()`, NOT interpreter index.** The runtime permutes both input and output indices away from name order, silently.
    - **Inputs** (`int64`, `[1,128]`): `serving_default_args_0:0` → `input_ids`, `serving_default_args_1:0` → `attention_mask`. Dispatch by parsing the `_N:0` suffix from `getInputTensor(i).name()`.
    - **Outputs** (`float32`): `:0` → preflight `[1,3]` `[search_required, search_not_required, ambiguous]`; `:1` → presence `[1,2]` `[no_extraction, has_extraction]`; `:2` → category `[1,6]` multi-label sigmoid `[personal_identity, preference, professional, interest, relationship, temporary_context]`. Dispatch by parsing the `:N` suffix from `getOutputTensor(i).name()`. Hardcoding indices ships a silent swap.
13. **Classifier tokenizer must match training-time `distilbert-base-uncased` exactly.** Different vocab (cased vs uncased), different sub-word splits, or a stale `vocab.txt` silently degrades the classifier with no error. The Android side bundles the 30,522-entry WordPiece vocab and lower-cases input. `WordPieceTokenizerFixtureTest` asserts byte-exact input_ids vs the Python tokenizer on 22 fixtures — if you re-export the model, re-run this test.
14. **Pre-flight thresholds are CONFIGURABLE per PRD §3.2.1.** Shipped via `:androidApp/src/main/assets/preflight_config.json`, read at startup. Currently **0.3 high / 0.15 low** — relaxed from the original 0.85 (→ 0.5 → 0.4 → 0.3) after on-device testing showed the v1.0 classifier under-firing on weather/sports/news/finance queries the vertical adapters handle cleanly (most recently a "latest news headlines" query scoring p_search_required=0.391, just under the 0.4 band). The asset is the runtime source of truth; `PreflightThresholds.DEFAULT` (still **0.5f** in code) only fires if the asset fails to load, and `CanonicalEvalTest`'s middle-band fixture (softmax ≈ 0.452) is pinned to that default — bump them together if you change it. Don't hard-code — that blocks telemetry-driven tuning, which is the documented path to closing the v1.0 §7 precision gap.
15. **Classifier .tflite sequence length is statically baked at export (currently 128).** Different lengths require re-export via `ct-export-litert --max-length N`.
16. **`ai-edge-torch` was renamed to `litert-torch` in 2025.** Use `import litert_torch` for new code; the export driver falls back to `ai_edge_torch` only for older saved sessions. Quantization recipes in `litert_torch.generative.quantize` are LLM-specific (not for encoder classifiers) — use `ai_edge_quantizer.Quantizer` with `MIN_MAX_UNIFORM_QUANT` for encoder INT8 weight-only quant.
17. **`models/` is gitignored — model cards live in `docs/`.** Keep the model card at `docs/preflight_memory_shared_vX.Y.Z_MODEL_CARD.md` so it tracks in git alongside the handoff note.
18. **Pre-flight classifier MUST run on `com.google.ai.edge.litert:litert:2.x` — NOT `org.tensorflow:tensorflow-lite` and NOT Play Services TFLite.** Both classic TFLite runtimes produce numerically broken outputs for our `ai-edge-quantizer` INT8 model (logits ~1500× the Python reference magnitude, every query collapsing to one class). No exception, just silently wrong arithmetic. `com.google.ai.edge.litert:litert:2.1.4` is the Android port of Python's `ai-edge-litert` — the runtime ai-edge-quantizer's export tooling targets. Different package (`com.google.ai.edge.litert.CompiledModel`), different native libs (`libLiteRt.so`). LiteRT-LM (Gemma 4) keeps its own runtime via `play-services-tflite-*`; both coexist on the classpath. **Class-collision workaround:** the litert AAR bundles `org.tensorflow.lite.*` and so does the transitive `tensorflow-lite-api`; AGP rejects duplicates. Resolved via `configurations.matching { ... }.configureEach { exclude(group = "org.tensorflow", module = "tensorflow-lite-api") }` in `:shared/build.gradle.kts` and `:androidApp/build.gradle.kts`.

The embedder uses the same runtime (#18) and same vocab as the classifier — `assets/vocab.txt` is byte-identical SHA-256 to bert-base-uncased's, so both engines share one tokenizer singleton via Hilt.

GPU delegate refuses both the classifier and embedder graphs (`BROADCAST_TO`, `EMBEDDING_LOOKUP`, `CAST INT64→FLOAT32` unsupported). CPU XNNPACK is the only path. Classifier p95 = 113 ms; embedder p95 = 41 ms on Pixel 7.

### Build, Android platform, framework gotchas

5. **`:shared` uses `com.android.kotlin.multiplatform.library`**, not the old `com.android.library` + `kotlin.multiplatform` combo (AGP 9 forbids it). Android config goes inside `kotlin { android { } }`.
6. **WorkManager's `SystemForegroundService` needs `foregroundServiceType` merged in our manifest.** Any worker that calls `setForeground(...)` will crash the worker process with `IllegalArgumentException: foregroundServiceType 0x1 is not a subset of 0x0` unless we declare the type via `tools:node="merge"` in `AndroidManifest.xml`. Currently `dataSync` for the model download.
7. **`POST_NOTIFICATIONS` must be requested at runtime on Android 13+.** Manifest permission alone is not enough — the OS silently suppresses every notification (including FGS-required ones) until granted. `MainActivity.ensureNotificationPermission` does this on first launch.
19. **AGP 9 KMP source-set DSL doesn't accept the closure form for catalog `Provider` deps.** `implementation(libs.foo) { exclude(...) }` inside `kotlin { sourceSets { androidMain.dependencies { ... } } }` fails. Workaround: configuration-level `configurations.matching { it.name.startsWith("androidMain") || ... }.configureEach { exclude(...) }` outside the `kotlin {}` block.
20. **SQLDelight `.sqm` files go alongside `.sq` files in the same package directory** (NOT a `migrations/` subdir). Schema version is auto-derived: with `N.sqm` present, `Schema.version` becomes `N + 1`. `verifyMigrations = true` REQUIRES a committed `.db` schema snapshot per prior version in `schemaOutputDirectory` (`src/commonMain/sqldelight/databases/`) — without one, code-gen itself fails.
    **Snapshot dance for a new migration:** (a) generate `<currentVersion>.db` FIRST via `./gradlew :shared:generateCommonMainMobileAgentDatabaseSchema` while the `.sq` files are still at the old state; (b) write the new `.sqm`; (c) update the `.sq` schema; (d) rebuild. Because `ALTER TABLE ADD COLUMN` appends to the end, the `.sq` file MUST declare new columns at the END of `CREATE TABLE`, otherwise `verifyMigrations` flags column-order drift.
21. **Hilt `@Provides` methods do NOT auto-fill default constructor parameters.** If a class has `@Inject constructor(..., dep: Foo = NoOpFoo)` AND it's constructed by a `@Provides` factory, the factory MUST explicitly pass the real `Foo` — otherwise the default fires and the dep is silently no-opped in production. M6 Phase C hit this for `TelemetryCounters` across multiple modules.
22. **For "do X whenever screen Y is visible" use `LifecycleResumeEffect(key)`, NOT `LaunchedEffect(key)`.** `LaunchedEffect` re-fires only on key change and misses background→foreground (the route key is unchanged). `LifecycleResumeEffect` re-fires on key change OR `ON_RESUME`. Pair with `onPauseOrDispose { job?.cancel() }`. Load-bearing for the aux (classifier + embedder) eager warm-up on Chat re-entry. Gemma is NOT eagerly warmed any more (PR #25 reversed M6 Phase B) — it loads on the first `InferenceSessionManager.generate()` call.
23. **Firebase SDK deps live in `:androidApp`, NOT `:shared/androidMain`.** Define the abstraction (`AnalyticsSink`, `SafeCrashReporter`) in `:shared/commonMain`; implement in `:androidApp/.../telemetry/` or `:androidApp/.../observability/`.

### Privacy, telemetry, redaction (M6)

24. **Firebase Crashlytics has NO `beforeSend` egress hook** (unlike Sentry). Redaction lives at every callsite, behind the `SafeCrashReporter` facade. Direct `FirebaseCrashlytics.getInstance(...).recordException(...)` outside the facade is a contract violation. The facade's `recordException` wraps in a `RedactedThrowable` (preserves stack trace + class name, scrubs message). **Never put user text in exception messages or breadcrumbs in the first place** — the redactor is defense-in-depth.
25. **Crashlytics non-fatals batch until next app launch.** `sendUnsentReports()` forces immediate upload — exposed via `SafeCrashReporter.flushPending()` for the debug button. Crashlytics dedupes non-fatals by exception class + top-of-stack signature; a re-record bumps the existing issue's events count rather than creating a new entry.
26. **Compose `liveRegion = Polite` on growing/streaming text fires on every content update**, so TalkBack re-reads the whole growing string. Do NOT put `liveRegion` on the streaming bubble. For one-shot announcements use `LocalView.current.announceForAccessibility(text)` from a `LaunchedEffect(messages.size)` that fires only on growth. `announceForAccessibility` is deprecated in API 36 but still the canonical primitive for this case.
27. **Counter telemetry uses a separate channel from text-aware loggers.** Memory pipeline classes inject BOTH a `logger: (String) -> Unit` (counts, IDs, accelerator names — safe for logcat) AND a `TelemetryCounters` (off-device-bound). DO NOT bridge logger → telemetry. `TelemetryPayloadBuilderTest` includes a load-bearing canary test seeding a unique marker string into `memories` and asserting it never appears in any built payload.
28. **Logcat tags for production loggers come from the DI module, NOT the class.** Preflight log lines tag `ClassifierModule`, not `PreflightRouter` — the tag is set in `ClassifierModule.providePreflightRouter`. Production diagnostic filter: `adb logcat -s EagerWarmUp:I InferenceSessionManager:I TelemetryWorker:I ChatViewModel:I ClassifierModule:I VerticalSearch:I BraveApi:I MemoryRetriever:I MemoryExtractor:I MemoryBackupController:I AndroidRuntime:E`. (`BraveApi` logs the actual outgoing Brave `q` for every request — GENERAL + site:-augmented NEWS/SPORTS/FINANCE — set in `SearchModule`; PR #37. As of PR #42 GENERAL/NEWS/SPORTS run `KtorBraveLlmContextClient`, so their line reads `llm-context q="…"` followed by the chunked raw `/llm/context` response dump; FINANCE stays on `KtorBraveSearchClient` (`q="…"`, see #37). Diagnostic logcat only, NOT telemetry, distinct from the redacting HTTP error logger.) **By invariant #27, `MemoryRetriever` only logs on the error path** — successful retrieval is counters-only. Silence is normal; logcat entries mean a failure branch fired.

### Search verticals (PR #23 routing; PR #27 STOCKS; PR #34 SPORTS→Brave; PR #35 FINANCE→Brave + STOCKS merged; PR #37 weather DWML + query-time location; PR #38 FINANCE quote card; PR #41 SPORTS→Brave LLM Context API; PR #42 GENERAL/NEWS→LLM Context, FINANCE stays /web/search; PR #43 relative-temporal force-fire; PR #47 NEWS multi-kind composite)

30. **STOCKS was removed in PR #35 — single-instrument quotes are now part of FINANCE.** *(Historical: M-era PR #27 added a dedicated `SearchSubtype.STOCKS` that resolved company → ticker via a two-call `StockLookupAdapter` against stockanalysis.com, because a 2B Gemma hallucinates tickers. PR #35 deleted that adapter and the `STOCKS` enum value once FINANCE moved to the Brave `site:` path — one Brave query like `nvidia stock price (site:bloomberg.com)` answers a single-instrument quote without the ticker round-trip.)* Consequence: `SearchSubtypeDetector` no longer has a `STOCKS_PATTERN`; the single-instrument coverage (`shares`, `price target`, plus the `stock`/`$TICKER`/`market cap` vocab that already matched) folds into `FINANCE_PATTERN`. There are no STOCKS `when` branches left in `VerticalPreferences`/`SearchSourcesScreen`. See invariant #31 for the Brave wiring and #33 for how PR #38 later re-introduced a (deterministic, non-LLM) stockanalysis.com fetch for single-instrument quote cards.
31. **SPORTS and FINANCE use the Brave `site:` path, NOT RSS (SPORTS PR #34, FINANCE PR #35).** **(PR #41 → PR #42: the `site:`-pinned `BraveSiteFilterAdapter` mechanics below are unchanged, but the endpoint moved per vertical. PR #41 pointed SPORTS at the Brave *LLM Context* endpoint (`/llm/context`); PR #42 moved GENERAL + NEWS onto it too. **FINANCE stays on `/web/search`** — its ticker resolution needs the `finance.yahoo.com/quote/<TICKER>/` URL `/llm/context` doesn't return. See #35 and #37.)** `VerticalSearchDispatcherFactory` wires `SearchSubtype.SPORTS` to `BraveSiteFilterAdapter(subtype = …)` — the same adapter NEWS uses — reversing its original M-era `FeedAdapter` (RSS) wiring. (FINANCE wires to `FinanceQuoteAdapter` with this same `BraveSiteFilterAdapter` as its fallback — see #33; the site-filter behavior below is FINANCE's fallback path.) RSS feeds only carry recent headlines and can't answer historical/specific queries ("who won the masters last year") or single-instrument quotes ("nvidia stock price"); a web search restricted to `site:espn.com` / `site:bloomberg.com` can, and for finance it does so in one call (subsuming the old STOCKS ticker resolver — see #30). `FeedAdapter` is now live for **WEATHER only** (see #32; it handles both Environment Canada RSS and US DWML). Consequence: sports & finance sources in `search_defaults.json` and the Settings Add-source dialog (`defaultKindFor`) are `BRAVE_SITE_FILTER`, with `endpointTemplate` holding the bare domain or a path-scoped filter (e.g. `bbc.com/sport`). The dispatcher runs exactly one adapter per subtype (`adapters[subtype] ?: generalAdapter`), so these queries no longer touch RSS at all; any leftover RSS-kind entry a user previously saved is silently ignored by `BraveSiteFilterAdapter`'s `BRAVE_SITE_FILTER` filter (falls back to an unfiltered Brave query). **(EXCEPTION — NEWS, PR #47: NEWS no longer maps to a single adapter. It maps to `NewsKindRoutingAdapter`, a composite that fans the NEWS site list out by kind — RSS/DWML/HTML/JSON to a `FeedAdapter(subtype = NEWS)`, BRAVE_SITE_FILTER to the `BraveSiteFilterAdapter` — and merges the two outcomes, so NEWS can exercise ALL five `SourceKind` parsers. Anti-pollution rule: the Brave side runs ONLY when ≥1 BRAVE_SITE_FILTER source exists, because `BraveSiteFilterAdapter` with zero domains issues an *unfiltered full-web* search and returns Success — which would contaminate a feed-only NEWS list. If only one side has sources its outcome passes through untouched (the default NEWS list is Brave-only, so the common path is unchanged); when both run, chips are concatenated/deduped/capped at 10 and the two `payload.json` blocks are wrapped in a parseable envelope `{"subtype":"news","query":…,"feeds":<feed.json>,"web":<brave.json>}`. Other verticals are still one-adapter.)** **SPORTS & FINANCE are single-source / single-citation-chip (PR #36):** their `search_defaults.json` lists exactly one domain per country, and the dispatcher wires them with `maxDomains = 1, maxCitations = 1` — `BraveSiteFilterAdapter` trims only the post-cache `payload.sources` (UI chips) via `SearchPostProcessor.limitCitations` (endpoint-agnostic — trims chips for both the `/web/search` and `/llm/context` payloads; see #37) so one site doesn't surface redundant chips. **`payload.json` (the model's `[SEARCH CONTEXT]`) keeps Brave's full top-N** — capping it instead starves the model (it then can't read e.g. the actual quote off a stock page). **NEWS caps citations at 10** (PR #42 follow-up; was uncapped) and ships two US default sources (`apnews.com` + `reuters.com`); its client runs `maxUrls = 10` (see #37) so up to 10 source chips can surface across the `(site:apnews.com OR site:reuters.com)` filter.
32. **WEATHER is rendered deterministically and NEVER consulted to the LLM (PR #37).** The vertical fetches a national source — Environment Canada RSS (CA) or `forecast.weather.gov` DWML (US, `SourceKind.DWML` + `DwmlParser`, mirroring `RssParser`'s `RssEntry` shape; PR #37 made US weather a SINGLE DWML source, dropping weather.com) — and `WeatherResponseFormatter` builds the bubble; the LLM is never asked (Gemma mangles numbers, can't read a feed, doesn't know location). **Location is resolved at query time, not stored at onboarding.** Onboarding now captures **country only** (`LocationPickerScreen` → `OnboardingViewModel.saveLocation(country)`; seeds default sources); `regionCode`/`city` are left empty. Per-query resolution order in `AgentLoop`: (a) city+state/province parsed from the query via `WeatherLocationResolver` against the bundled `LocationCatalog`; else (b) a saved location memory; else (c) a deterministic prompt asking for city + state/province. The resolved city's country selects the national source via `DefaultSiteResolver`, so "weather in Toronto" works for a US-onboarded user. **The classifier under-fires on bare forecast queries, so the weather path force-fires** outside the classifier: it triggers when the query names a resolvable catalog city OR matches the tight whole-message `BARE_WEATHER_PATTERN` ("what's the weather", "what's the forecast"). The saved-location fallback (b) is reachable ONLY via the bare pattern — a WEATHER query naming an unresolvable place or a general question ("what's the weather typically like in England") defers to the classifier instead of being hijacked. When the city came from the user's own words, `AgentLoop` emits `AgentEvent.Done.locationToRemember` and `ChatViewModel` surfaces a Save/Dismiss consent card via `MemoryExtractor.proposeLocationMemory` (NOT a force-save — same consent path the classifier uses post-PR #7; deduped, no re-prompt for a known place). `locations.json` covers all CA provinces/territories + 50 US states + DC with GPS coords; new cities work automatically (endpoints are coordinate-driven).
33. **Single-instrument FINANCE quotes render as a deterministic card via stockanalysis.com, bypassing the LLM (PR #38).** `VerticalSearchDispatcherFactory` wires FINANCE to `FinanceQuoteAdapter(fallback = BraveSiteFilterAdapter)` (NOT the site-filter adapter directly as #31 describes for the general case). Flow: (1) a Brave `site:finance.yahoo.com` web search resolves the ticker — the result URL carries `/quote/<TICKER>/`; (2) GET `stockanalysis.com/stocks/<ticker>/` and `StockAnalysisParser` extracts the live quote from the page's minified `quote:{p,c,cp,h,l,v,h52,l52,…}` blob + `marketCap`/`peRatio`; (3) `StockResponseFormatter` renders a `"subtype":"stock_quote"` card (price, signed change/%, day & 52-wk range, mkt cap/P/E/vol, source · as-of). On ANY miss (no ticker, fetch/parse failure) the adapter returns the Brave snippet outcome unchanged → LLM fallback (pre-#38 site-filter behavior). NOT cached (a cached price is a stale price). `AgentLoop` has a FINANCE direct-render block after the WEATHER block. **This partially re-introduces the stockanalysis.com dependency #30 said PR #35 removed** — but as a deterministic page-parse + card, not the old `StockLookupAdapter` ticker resolver feeding page text to Gemma. *(An earlier #38 iteration used Brave's `/web/rich` callback for the quote; that was reverted in favor of the stockanalysis.com parse — `docs/BRAVE_SPIKE.md` §4 still describes the /web/rich shape.)*
35. **SPORTS runs Brave's LLM Context endpoint (`/res/v1/llm/context`), NOT `/web/search` (PR #41).** It returns pre-extracted page content (prose, tables) instead of ≤200-char index snippets — richer grounding for "who won…/scores" queries. Wiring: `VerticalSearchDispatcherFactory` keeps SPORTS on the `site:`-pinned `BraveSiteFilterAdapter` (#31) but points it at a SECOND `SearchService` (Hilt `@SportsSearch`) backed by `KtorBraveLlmContextClient` — same `BraveSearchClient` interface (so key/cache/counters are reused), different endpoint; the `grounding.generic[]` response is parsed by `LlmContextPostProcessor` into the same `FormattedSearchPayload`. Same Brave Search plan + `X-Subscription-Token`, single round-trip. **The two `SearchService`s share one `SearchCacheDao`; the SPORTS one passes `cacheNamespace = "sports:"`** so an identical query string can't serve a `/web/search` payload to a SPORTS turn (the prefix tokenizes as a non-time-sensitive word, leaving `SearchCacheClassifier` TTL bands intact). **Load-bearing tuning — do NOT revert without re-reading the PR #41 thread (it's all there because Gemma E2B mis-transcribes digits, e.g. "114"→"1114", from a number-dense context; confirmed empirically on-device):** (a) SPORTS's `KtorBraveLlmContextClient` is built with `maxUrls = 1` (PR #42 made `maxUrls` a per-vertical ctor param, default 1; GENERAL passes 3 and NEWS passes 10 — see #37) — SPORTS is `site:`-pinned to one domain (espn.com US / tsn.ca CA per `search_defaults.json`), and extra same-domain URLs were noise (a VideoObject blob, a standings table); (b) `LlmContextPostProcessor` drops snippet chunks that are JSON-serialized structured data (open with `{`/`[`), keeping prose; (c) search-grounded turns decode greedy + drop prior history — see #36. **PR #41 first shipped SPORTS *unpinned* (trusting the endpoint's own ranking); that was reversed within the same PR** after on-device logs showed it ranked nba.com video clips + a sportingnews schedule table above espn.com. The deterministic-score-card route (regex-parse + render, bypass the LLM like #32/#33) is the only thing that *guarantees* correct digits; deferred as high-effort/brittle for unstructured sports prose. *(Open question "does `/llm/context` honor `site:`?" — confirmed YES on-device.)* `KtorBraveLlmContextClient` dumps the raw Brave response to logcat (`BraveApi` tag, chunked) so the model's input is inspectable. **(PR #42: NEWS/GENERAL moved to `/llm/context` too; FINANCE stays on `/web/search`. See #37.)**
38. **Relative-temporal queries force-fire search inside `PreflightRouter`, NOT `AgentLoop` (unlike the WEATHER force-fire #32) (PR #43).** The shipped classifier under-fires on now-relative queries — "who won the super bowl last year" scored `p_search_required=0.175` → middle band → on-device Gemma, which has a fixed knowledge cutoff and answers stale. `RelativeTemporalDetector` (`search/`) matches PAST/PRESENT/FUTURE relative phrases (topic-agnostic) and widens the router gate to `pSearch > highBand || temporalDetector.matches(query)`, so the existing rewriter (resolves "last year" → a concrete year, #14 area) + `SearchSubtypeDetector` path runs (e.g. "super bowl" → SPORTS). It lives in the router (not AgentLoop like WEATHER) **precisely because temporal force-fire WANTS the rewriter + subtype detection that the WEATHER force-fire bypasses** — the literal phrase the rewriter doesn't resolve ("last hour", "3 days ago", "recently") just passes through to Brave, which is fine. **PR #43 also added the rewriter's *future* date rules** — `tomorrow` (+ morning/afternoon/evening/night), `next week`, `next month`, `next year` (mirroring the existing `last`/`this` families in `QueryRewriter.dateTimeRules`) — so "next month" in May 2026 rewrites to "June 2026" rather than riding through literally. The detector **excludes absolute dates** ("in 2019", "Jan 5 2020") since the LLM can answer fixed-point history; bare "now" is excluded too (false hits: "now that…", "know how"). The `searchAvailableProvider` short-circuit to `SearchDisabled` still runs first, and `RewriterAbort` is still possible (e.g. "did my team win last week" with no memory). **Accepted false positives** ("how are you today" → a wasted Brave round-trip + GENERAL subtype) are tracked via `preflight_temporal_force_total` — a subset of `preflight_high_band_total`, incremented only when `forceTemporal && pSearch <= highBand` (i.e. the band alone would NOT have fired) — so the trigger can be tuned from telemetry (#14 ethos). Logcat shows ` forced=temporal` on the `ClassifierModule` FireSearch line (#28).

### Prompt assembly

34. **The `[SEARCH CONTEXT]` block + `PREFLIGHT_NOTICE` ride on the CURRENT user turn, NOT the system instruction (PR #39).** `PromptAssembler.appendSearchContext` appends them to the tail `HistoryMessage` (the message sent via `sendMessageAsync`); `buildSystemInstruction` no longer takes `searchContext`. **Don't move them back into the system prompt** — that reintroduces the disable-then-enable-search bug: with search off, a recency query gets the correct "I don't have real-time data" refusal; the user enables search and re-asks; search fires and results inject, but Gemma repeats the refusal. The system instruction sits at the far front of the context window while the prior refusal is the assistant turn right before the current query (next to the generation point), so a 2B model anchors on its own recent refusal and ignores distant evidence — and the notice forbidding the refusal lost the same recency battle. Riding the evidence on the current user turn (canonical RAG placement) makes it the most-recent thing read, so fresh results win. The notice's "block above" wording stays correct (it renders after the block within the same message). Defensive: if the tail isn't a `USER` turn, `appendSearchContext` falls back to leaving history unchanged rather than misattributing evidence. The history-hygiene alternative (pruning the stale refusal) was rejected — `ChatMessage.Assistant` has no "used search" flag, so detection would rely on brittle phrase/locale matching. Tests assert the block lands on `request.history.last().text`, NOT `systemInstruction` (`PromptAssemblerTest`, `AgentLoopPreflightTest`, `AgentLoopMemoryTest`); `CanonicalEvalTest.checkPromptStructure` widens its haystack to system instruction + tail user turn. **(PR #41: on a search-grounded turn the prior history — including the stale refusal this invariant fought on recency grounds — is now dropped entirely, so the block rides on what is then the ONLY turn. See #36; the recency mechanism here is subsumed but the placement and `appendSearchContext` defensive fallback are unchanged.)**
36. **Search-grounded turns drop prior conversation history AND decode near-greedy (PR #41).** Both gate on the same signal — a `[SEARCH CONTEXT]` block is present (so: GENERAL/NEWS/SPORTS; WEATHER/FINANCE render deterministically and never reach the LLM). (a) **History scoping:** `PromptAssembler.assembleStructured` calls `scopeToCurrentTurn` when `searchContext` is non-blank, keeping only the trailing USER turn — earlier turns are omitted from THIS generation. `AgentLoop` still PERSISTS full history for follow-ups; only what the model reads this turn is scoped. (b) **Greedy decoding:** `AgentLoop` sets `GenerationRequest.sampling = SamplingParams.GREEDY`; `LiteRtInferenceEngine` builds its per-call `SamplerConfig` from `request.sampling ?: typed.config`. `GREEDY` is `topK = 1` (argmax) with `temperature = 1.0`, **NOT `0.0`** — `SamplerConfig` exposes no explicit greedy mode, and a `0.0` temp risks a `logits/temp` divide-by-zero or a "temp disabled → stochastic fallback" special-case in the native sampler that silently defeats greedy; `topK = 1` is the real lever, temperature is just held somewhere safe. Why both: a 2B model under the default temp 0.7 with a growing history bleeds/perturbs digits when copying figures from the evidence ("110 vs 112" → "1110 vs 1112" once history grew; established on-device). Greedy + scoping reduce but do NOT eliminate digit errors (see #35 — the model floor; the deterministic card is the only guarantee). `AgentLoop`'s "sending to engine" log prints `historyTurns=N sampling=greedy(topK=1,temp=1.0)|default` for on-device verification. Tests: `PromptAssemblerTest` asserts prior history is dropped on a search turn (and kept on a non-search turn); `AgentLoopPreflightTest` asserts `request.sampling == SamplingParams.GREEDY` on a search turn, `null` on a middle-band turn.
37. **GENERAL/NEWS/SPORTS run `/llm/context` (each its own URL budget); FINANCE deliberately stays on `/web/search` (PR #42).** PR #42 generalized #35 — but only to the verticals whose results the LLM *reads*, and with a per-vertical `SearchService` (each its own client tuning + cache namespace):
    - **GENERAL** → default `SearchService`, `KtorBraveLlmContextClient(maxUrls = 3)`, `cacheNamespace = "ctx:"`. (`maxUrls`/`maxTokens`/`maxSnippetsPerUrl` became ctor params, default 1/1800/6 preserving SPORTS; GENERAL is unpinned so >1 URL helps — hence 3.)
    - **NEWS** → `@NewsSearch`, `KtorBraveLlmContextClient(maxUrls = 10)`, `"news:"`; the NEWS `BraveSiteFilterAdapter` sets `maxCitations = 10` so up to 10 source chips surface. Ships two US defaults (`apnews.com` + `reuters.com`); `maxDomains` stays 3 so the OR filter covers both plus any source the user adds. (PR #42 follow-up.) **(PR #47: that `BraveSiteFilterAdapter` is now wrapped inside `NewsKindRoutingAdapter` — see #31 — which also routes any non-Brave NEWS sources through `FeedAdapter`. The merged citation cap stays 10.)**
    - **SPORTS** → `@SportsSearch` `SearchService`, `KtorBraveLlmContextClient(maxUrls = 1)`, `"sports:"` (#35 — single clean source).
    - **FINANCE** → `@FinanceSearch` `SearchService`, `KtorBraveSearchClient` (`/web/search`), `"fin:"`. **FINANCE must NOT use `/llm/context`:** `FinanceQuoteAdapter.resolveTicker` parses the ticker out of a `finance.yahoo.com/quote/<TICKER>/` result URL (#33), but `/llm/context` returns content-article URLs (`/markets/stocks/articles/…`) with no `/quote/` segment — so ticker resolution silently fails, the deterministic stockanalysis.com card never fires, and every finance query falls through to the raw-snippet LLM path. **An earlier PR #42 iteration moved FINANCE onto `/llm/context` and hit exactly this** (logcat `[vertical:FINANCE] no ticker in Brave results — snippet fallback`); reverted by giving FINANCE its own `/web/search` service. Because of this, `KtorBraveSearchClient` and `SearchPostProcessor.format` are **live, not deprecated** — they back the FINANCE path.
    **Cache:** `"ctx:"` on the default service drops pre-#42 `/web/search`-shaped rows (cached under the empty namespace) — payload *type* is identical (`FormattedSearchPayload`) so there's no deserialization break; the prefix just invalidates stale-shaped content instead of waiting out the ≤1h TTL. Each client gets a distinct namespace (`"ctx:"`/`"news:"`/`"sports:"`/`"fin:"`) so the response shapes + URL budgets never mix in the shared `SearchCacheDao`. **Wiring:** `VerticalSearchDispatcherFactory.create` takes `searchService` (GENERAL), `sportsSearchService`, `financeSearchService`, and `newsSearchService`; `VerticalSearchModule` injects the four qualified `SearchService`s. `BraveSiteFilterAdapter`, `LlmContextPostProcessor`, `SearchPostProcessor.limitCitations`, and the greedy/scoping path (#36) are endpoint-agnostic and **unchanged** — this was DI wiring + one ctor parameterization. Tests: `KtorBraveLlmContextClientTest` (new — MockEngine, asserts injected `maxUrls`/budget params + `/llm/context` path + auth header reach the wire), `SearchServiceTest` (the `"ctx:"` prefix bypasses legacy un-namespaced rows). WEATHER (#32) and the single-instrument FINANCE quote card itself (#33) never touch Brave.

### Vision / image input (PR #48)

39. **Image input is gated at engine init, sent on the current turn, and bypasses preflight/search.** Vision needs `EngineConfig.visionBackend` + `maxNumImages` set — it CANNOT be enabled per-request once the engine is loaded. `LiteRtInferenceEngine.newEngine` sets `visionBackend = Backend.GPU()` + `maxNumImages = 1` when `InferenceConfig.enableVision` is true; `InferenceSessionManager`'s chat-load default flips it true (the model is keyed on `modelPath`, so the first loader's config wins — the M0 spike uses its own `Engine`, unaffected). The image is a **downscaled JPEG `ByteArray`** carried on `HistoryMessage.imageBytes` / `ChatMessage.User.imageBytes` / `AgentTurnInput.imageBytes`; the engine sends the current user turn as `Message.user(Contents.of(Content.ImageBytes(bytes), Content.Text(text)))` via `sendMessageAsync`. **`Content.ImageBytes` (raw bytes), NOT `Content.ImageFile` (absolute path)** — the Photo Picker hands back a `content://` Uri, not a filesystem path, and an on-disk copy would be a needless second representation. **What the MODEL sees is trailing-turn-only:** only the current user turn carries bytes to the engine — the engine's history path (`toLiteRtMessage`) is text-only and `PromptAssembler.assembleStructured` strips `imageBytes` from every non-trailing turn, so a historical image is never re-fed to the LLM (sending prior images each turn would blow context/memory). **DISPLAY persistence (PR #49 — no longer fully ephemeral):** the downscaled JPEG IS persisted as a `messages.image_bytes` BLOB (migration `5.sqm`, v5→v6) and round-trips through `ChatMessage.User.imageBytes`, so a resumed conversation re-renders the photo in the bubble (`ChatViewModel.send` now persists it WITH bytes; `UiMessage.User.imageBytes` → `UserBubble` decodes on demand via `produceState`, bounded by LazyColumn windowing so a long thread doesn't hold every bitmap). Cleanup is free — FK `ON DELETE CASCADE` drops the BLOB with the conversation/turn (and on `deleteOldestPair` truncation); no orphan-file logic. **Storage ≠ what the model sees**: the BLOB exists purely for re-display; `AgentLoop` still writes a text-only `ChatMessage.User` into `turnMessages` for in-memory history. **An image turn skips the deterministic clock/todo/memory short-circuits AND preflight/search** (`AgentLoop`'s `hasImage` gate → `FallThrough(MiddleBand)`, `forceWeather` suppressed, warm sampling kept) — the question is about the photo, not the web. UI: Android Photo Picker (`ActivityResultContracts.PickVisualMedia`, no permission) → `ImagePreprocessor` decodes + downscales the longest edge to ~768 px (Gemma's vision size) via `ImageDecoder` (auto-applies EXIF orientation) and JPEG-encodes; no Coil dependency. `StubInferenceEngine` echoes `[stub] received image of N bytes`. Logcat: `[turn] image attached — skipping preflight/search` (`AgentLoop`), `vision enabled: visionBackend=GPU …` + `current user turn carries image: N bytes` (`LiteRtInferenceEngine`). Benign noise on Pixel 7: native `litert: No dispatch library found / Failed to initialize Dispatch API` is LiteRT-LM probing for an absent NPU vendor delegate; it falls back to the GPU vision path and succeeds. **Suppressing the `litert` spam:** it's native (emitted from `litert_dispatch.cc`/`dispatch_delegate.cc` in the bundled lib) so it CAN'T be silenced from app code — the dispatch probe is internal to LiteRT-LM's accelerator init with no public toggle. Mute it at logd instead: `adb shell setprop log.tag.litert SILENT` (persists until reboot — re-run after a device reboot or adb re-pair), or filter per-invocation with `adb logcat litert:S`. The whitelist diagnostic filter in invariant #28 already excludes it.
40. **LiteRT-LM and the classifier runtime ship colliding `libLiteRt.so` — pin BOTH to `litert:2.1.5` + `pickFirst`, never bump them independently.** 0.10.2 statically linked its LiteRT core into `liblitertlm_jni.so` (fully isolated from the `com.google.ai.edge.litert` classifier/embedder runtime — the basis of invariant #18). **LiteRT-LM 0.11.0+ ships the core as standalone `libLiteRt.so` + `libLiteRtClGlAccelerator.so`**, which share a SONAME with `com.google.ai.edge.litert`'s same-named libs. Android packages exactly one `.so` per name, and the two products' builds are NEVER byte-identical, so this is a real conflict, resolved in `androidApp/build.gradle.kts` via `packaging { jniLibs { pickFirsts += ["**/libLiteRt.so", "**/libLiteRtClGlAccelerator.so"] } }`. **The two are NOT freely interchangeable** (learned the hard way in PR #48): (a) LiteRT-LM's `libLiteRt.so` LACKS the `Java_com_google_ai_edge_litert_*` JNI symbols the classifier's Java API needs → forcing LiteRT-LM's copy crashes the classifier with `UnsatisfiedLinkError: Environment.nativeCreate`; (b) `litert:2.1.4`'s libs made `liblitertlm_jni.so` 0.12.0 **SIGSEGV in GPU init**. **`litert:2.1.5`'s libs are the one combination that serves both** — they carry the classifier JNI symbols AND a core `liblitertlm_jni.so` 0.12.0 tolerates; `pickFirst` lands on litert's copies (alphabetically first), and both runtimes work. So **`litertlm` and `aiEdgeLitert` versions are coupled** — bumping LiteRT-LM means re-checking that the litert version's `libLiteRt.so` still satisfies both the classifier (sane preflight, no UnsatisfiedLinkError) and the LLM (no SIGSEGV) on-device. Verified on Pixel 7: `FireSearch p=0.418 subtype=NEWS` + correct image description, no crash.

## Build & run

```bash
# Build + install on connected Pixel 7
cd android-app
./gradlew :androidApp:assembleDebug
./gradlew :androidApp:installDebug

# Dev iteration without loading the real Gemma model (StubInferenceEngine)
./gradlew :androidApp:installDebug -PuseStubEngine=true

# Launch / M0 spike harness
adb shell am start -n com.contextsolutions.mobileagent.debug/com.contextsolutions.mobileagent.app.MainActivity
adb shell am start -n com.contextsolutions.mobileagent.debug/com.contextsolutions.mobileagent.app.spike.SpikeActivity
```

### Skip the Gemma download during dev iteration

```bash
adb push gemma-4-E2B-it.litertlm /data/local/tmp/
adb shell run-as com.contextsolutions.mobileagent.debug \
  sh -c 'mkdir -p files/models && cp /data/local/tmp/gemma-4-E2B-it.litertlm files/models/'
```

Production read path: `filesDir/models/gemma-4-E2B-it.litertlm` (internal storage, written by `ModelDownloadWorker`).

### Wireless adb (Pixel 7's USB has been unstable)

Pair once, then re-`connect` after each phone reboot (the connection port changes; the pairing persists). Phone: Settings → System → Developer options → Wireless debugging.

```bash
adb pair <phone-ip>:<pairing-port>     # one-time, takes the 6-digit code
adb connect <phone-ip>:<connect-port>  # after reboot or WiFi drop
```

### Classifier training pipeline (M3 / M4 / WS-14)

```bash
cd classifier-training
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"           # gen, review, dedup, stats CLIs
pip install -e ".[training]"      # adds torch/transformers/litert-torch (CUDA)
```

Reproduce the v1.0 classifier (~10 min on RTX 5090): `ct-train-classifier` → `ct-eval-classifier` → `ct-export-litert`. Generation is Ollama-backed by default (`qwen3.5:9b`); set `CT_GEN_BACKEND=claude` + `ANTHROPIC_API_KEY` for the Claude path. Full CLI list in `classifier-training/pyproject.toml`.

WS-14 regression gate (run before any new `.tflite` lands in `models/`):

```bash
ct-regression-check --ckpt ../eval/runs/<ts>/best.pt
ct-regression-check --skip-eval --ckpt path/to/metrics.json   # hosted-CI flow
```

Exit codes: 0 PASS / 1 SHA-mismatch / 2 regression / 3 infra error.

### Instrumentation tests (Pixel 7)

```bash
./gradlew :androidApp:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=com.contextsolutions.mobileagent.classifier.ClassifierLatencyBenchmark
```

Other useful classes: `ClassifierEndToEndTest`, `EmbedderEndToEndTest`, `MemoryRetrievalLatencyBenchmark`.

### Secrets

`secrets.properties` lives at `android-app/secrets.properties` (next to `settings.gradle.kts`), NOT the repo root. Gradle's `rootProject` is `android-app/`, so `rootProject.file(...)` resolves there; `mobile-agent/secrets.properties` is silently ignored. See `android-app/secrets.properties.example`.

## Working norms

- **Don't commit unless explicitly asked.** The user reviews diffs and decides.
- **Read the official integration guide BEFORE introspecting the JAR.** `https://ai.google.dev/edge/litert-lm/android` documents intended usage (tool registration, multi-turn conversations) that can't be reverse-engineered from API signatures alone. In M2 we burned several iterations replaying conversation history through `initialMessages` because we hadn't read the "reuse the same Conversation" guidance. Read the docs first, then `javap -cp <jar> -p com.google.ai.edge.litertlm.X` to pin down exact signatures.
- **Keep all LiteRT-LM types behind `LiteRtInferenceEngine`.** The `InferenceEngine` interface in `commonMain` is the seam; don't import `com.google.ai.edge.litertlm.*` anywhere else.
- **Documentation hygiene:** when a Phase 1 decision changes, update both `PHASE1_PLAN.md` and `M0_DECISION_MEMO.md`. Don't rely on commit messages alone.
- **Brief responses.** The user reads diffs and code; don't recap them in prose.
