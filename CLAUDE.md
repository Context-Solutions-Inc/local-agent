# Context for Claude

Auto-loaded every session. Goal: don't make a fresh-context Claude rediscover what M0–M6 already cost time to learn.

## Always read first

Before suggesting architecture, scope, or code structure:

1. `PRD.md` — product spec (locked, ground truth)
2. `PHASE1_PLAN.md` — milestone plan + per-M status
3. `SYSTEM_PROMPT.md` — system prompt construction spec
4. `docs/M0_DECISION_MEMO.md` — ratified hardware/runtime decisions

On demand (when the work touches that area):
- `docs/M{3,4,5,6}_PLAN.md` / `docs/M{3,4,5,6}_*_HANDOFF.md` — per-milestone logs, decisions, deferred items
- `docs/OLLAMA_REMOTE_LLM_PLAN.md` — remote Ollama chat LLM (PR #56, see #44)
- `docs/DESKTOP_PORT_PLAN.md` / `docs/DESKTOP_LLAMA_SERVER_PLAN.md` — desktop port + LLM runtime
- `docs/JOBS_PLAN.md` §0 — jobs feature as-built (#49–52)
- `docs/DESKTOP_PACKAGING.md` — desktop build/install/headless deploy (#53)
- `docs/MACOS_BUILD.md` — macOS (Apple Silicon) local build/run/test + the per-PR `desktop-test.yml` CI gate (PR #34)
- `docs/VOICE_IO.md` — voice dictation (STT) + read-aloud (TTS), Android + desktop/Piper (#42, #48)
- `docs/ANYWHERE_ACCESS_PLAN.md` — paid relay subscription as-built + local E2E test steps (#54, PR #74 / secure-gateway PR #6)
- `docs/preflight_memory_shared_v1.0.0_MODEL_CARD.md` — classifier eval + weaknesses
- `CLASSIFIER_DATASETS.md` — pre-flight + memory extractor dataset spec

## Project at a glance

On-device AI assistant. **Pixel 7 + Android 16 only** for Phase 1; desktop (Linux/macOS/Windows) shipped via the Compose port. iOS stubbed for Phase 2.

- **Architecture:** KMP `:shared` agent core + Compose-Multiplatform `:ui` (every screen incl. Chat) + thin `:androidApp` / `:desktopApp` shells. DI is **Koin** (Hilt + KSP fully removed). `main` is the CMP+Koin reality.
- **Toolchain:** JDK 17, Gradle 9.3.1, AGP 9.1.1, Kotlin 2.3.21, Koin 4.2.1.
- **LLM runtime (Android):** LiteRT-LM 0.12.0 (`com.google.ai.edge.litertlm:litertlm-android`). Gemma 4 E2B (`gemma-4-E2B-it.litertlm`, 2.58 GB) — text + image. **Downloaded first-run from the public models CDN (`downloads.contextsolutions.com/models`, PR #28) with no auth (PR #22 — was the gated HF repo `litert-community/gemma-4-E2B-it-litert-lm`).** GPU via Play Services TFLite OpenCL on Mali-G710; CPU fallback. E4B ruled out (LMKD thrash on 8 GB). LiteRT-LM ≥0.11 needed for the vision encoder (couples to classifier runtime, #40).
- **LLM runtime (Desktop):** llama.cpp's `llama-server` as a localhost-HTTP subprocess (`LlamaServerInferenceEngine`, NOT a JNI binding). Gemma 4 E2B GGUF (`gemma-4-E2B-it-Q4_K_M.gguf`) + mmproj-F16; text + image via `/v1/chat/completions` → `mtmd`. **GGUF + mmproj download first-run from the public models CDN (`downloads.contextsolutions.com/models`, PR #28) with no auth (PR #22 — was the HF repo `unsloth/gemma-4-E2B-it-GGUF`).** Prebuilt server pinned (`LlamaServerRelease.TAG`), downloaded first-run, CPU or **Vulkan** variant.
- **Remote LLM (both platforms, optional):** point the *chat LLM* at a remote server (Settings → **Remote LLM Connection**): **Ollama** (default, `host:port`, plain HTTP, optional SSL) **or generic OpenAI-compatible** (PR #73 — OpenAI/OpenRouter/LM Studio/vLLM/llama-server/LocalAI; full base URL incl. path, always HTTPS, API key required). `RoutingInferenceEngine` (commonMain) wraps the local engine, routes to `OllamaInferenceEngine` (pure-Ktor, OpenAI-compatible `/v1/chat/completions`, text + image) when **active** (configured + on/off switch on) + reachable, else local. Classifier/embedder/search/memory always stay on-device. See #44.
- **Classifier + embedder runtime:** `com.google.ai.edge.litert:litert:2.1.5` — a *different* runtime from LiteRT-LM (#18, #40).
- **Classifier:** shared DistilBERT-base encoder + 3 task heads, INT8 → `preflight_memory_shared_v1.0.0_int8.tflite` (67.7 MB), **CDN-downloaded on first run** into `filesDir/models/` (PR #3 — no longer bundled in the APK; see #61). 3 outputs/pass.
- **Embedder:** all-MiniLM-L6-v2 INT8 (23.5 MB), **CDN-downloaded on first run** (PR #3). Mean-pool + L2-norm baked in; one 384-dim vector out.
- **Status:** M0–M6 complete (`PHASE1_PLAN.md` §5). M7 (closed beta → Play Store) not started. Desktop port + llama-server rework + remote-Ollama all merged to `main`. **PR #22 (merged) — see invariants #71–#76:** continuous timer ring + "+1 min", mobile model-load banner, desktop My List + bidirectional sync, Remote-LLM/Web-Search default OFF, HuggingFace fully removed (all models on the models CDN, no auth; host is `downloads.contextsolutions.com/models` since PR #28) + telemetry/HF/Brave onboarding steps removed, Search-sources country picker, My List "show #N". Detailed PR history lives in auto-memory (`MEMORY.md`), not here.

## Hard invariants

Surfaced the hard way — don't rediscover. Numbering is **stable**; code/comments reference these by number.

### Inference runtime (LiteRT-LM, Gemma)

1. **Every LiteRT-LM call must run off the main thread.** `Engine.initialize()` blocks 4–8 s and will ANR. `LiteRtInferenceEngine` wraps in `Dispatchers.IO`.
2. **`GenerationRequest.maxTokens` is a no-op.** No per-call cap; cancel the Flow at the parser layer to stop early.
3. **`Backend.GPU()` requires `play-services-tflite-gpu:16.4.0` at runtime.** Without it GPU init throws `Cannot find OpenCL library`; old Play Services fall back to CPU via `LiteRtInferenceEngine.tryInitialize`.
4. **Pixel CDEV throttling drops GPU clocks before `PowerManager.currentThermalStatus` reflects it.** Infer throttling from measured tok/s drift, not the high-level API.
8. **Tool calling MUST go through LiteRT-LM's structured channel** — register via `ConversationConfig.tools` (`tool(OpenApiTool)`). Text-only schemas in the system prompt do NOT unlock tool-use mode (model defaults to "I don't have real-time data" refusals).
9. **Reuse the same `Conversation` across the multi-step tool-call cycle** (per https://ai.google.dev/edge/litert-lm/android): `sendMessageAsync(userText)` → if `Message.toolCalls` populated, run tool → `sendMessageAsync(Message.tool(...))` on the **same** conversation. Re-creating per step + replaying via `initialMessages` breaks Gemma's call↔response correlation. `LiteRtInferenceEngine.generate` drives this loop via a `ToolDispatcher` callback.
10. **`Content.ToolResponse(name, response)` needs a structured payload, not a JSON string.** Gson quotes a `String` → Gemma sees a blob. Engine parses with kotlinx.serialization → `List<Map<String, Any?>>` (`LiteRtInferenceEngine.parseAsStructured`).
11. **Don't pre-template `<start_of_turn>…<end_of_turn>` markers.** LiteRT-LM applies Gemma's chat template internally. `PromptAssembler.assembleStructured` returns markers-free.
29. **`Conversation.cancelProcess()` is the ONLY way to stop an in-flight decode.** `Job.cancel()` closes the Flow but the native worker decodes to end-of-turn, holding GPU/CPU and freezing the UI. `LiteRtInferenceEngine.bindCancellation` fires `cancelProcess()` on parent cancel (finally block, before `conversation.close()`), driven by the watchdog: `InferenceSessionManager.forceUnload(UnloadReason.MainThreadWatchdog)`. **`forceUnload` cancels in-flight generations ONLY for `MainThreadWatchdog`**; `LowMemory`/`TrimMemory`/`Manual` defer (cutting a live turn is worse UX). Best-effort if the GPU is wedged. (Not in public docs.)
44. **The remote-LLM path lives entirely behind the `InferenceEngine` seam — never special-case it above the seam.** `RoutingInferenceEngine` (`single<InferenceEngine>`, both platforms) sits **below** Android `InferenceSessionManager` / desktop `WarmModel` and decides backend **once per `loadModel`** (active+reachable→remote, else local), tagging the `RoutedHandle`; agent loop/session/service/watchdogs unchanged. Resident handle drops (→ next turn re-decides) on (a) Settings change (`OllamaPreferences.configFlow()`) and (b) `OllamaConnectionMonitor` offline/online. Two backend types via `OllamaConfig.serverType` (`OLLAMA`/`OPENAI`), both behind the same `OllamaInferenceEngine`/`OllamaClient`/`OllamaPreferences` names (extend, don't rename); gate is **`isActive = enabled && isConfigured`**. **Full rule — URL shapes, SSL, `keep_alive`, API-key, on/off switch, diagnostics, `Accelerator.REMOTE`, cleartext, un-timed stream: `docs/OLLAMA_REMOTE_LLM_PLAN.md` (+ its PR #73 section).**

`MarkerFunctionCallParser` is legacy (pre-#8 text-marker workaround) — unused in production but still tested.

### Classifier + embedder runtime (ai-edge-litert)

12. **The classifier .tflite has 2 inputs + 3 outputs identified by `tensor.name()`, NOT interpreter index** (runtime silently permutes both).
    - **Inputs** (`int64 [1,128]`): `serving_default_args_0:0`→`input_ids`, `serving_default_args_1:0`→`attention_mask`. Dispatch on the `_N:0` suffix of `getInputTensor(i).name()`.
    - **Outputs** (`float32`): `:0`→preflight `[1,3]` `[search_required, search_not_required, ambiguous]`; `:1`→presence `[1,2]` `[no_extraction, has_extraction]`; `:2`→category `[1,6]` multi-label sigmoid `[personal_identity, preference, professional, interest, relationship, temporary_context]`. Dispatch on the `:N` suffix of `getOutputTensor(i).name()`.
13. **Classifier tokenizer must match training-time `distilbert-base-uncased` exactly.** Wrong/stale vocab silently degrades it with no error. Android bundles the 30,522-entry WordPiece vocab, lower-cases input. `WordPieceTokenizerFixtureTest` asserts byte-exact input_ids — re-run if you re-export.
14. **Pre-flight thresholds are CONFIGURABLE (PRD §3.2.1) via `assets/preflight_config.json`, read at startup.** Currently **0.3 high / 0.15 low** (relaxed from 0.85 — v1.0 under-fires on weather/sports/news/finance the verticals handle). The asset is the runtime source of truth; `PreflightThresholds.DEFAULT` (**0.5f**) only fires if the asset fails to load, and `CanonicalEvalTest`'s middle-band fixture is pinned to it — bump together. **Don't hard-code** (blocks telemetry-driven tuning).
15. **Classifier .tflite sequence length is statically baked at export (128).** Different lengths need `ct-export-litert --max-length N`.
16. **`ai-edge-torch` was renamed `litert-torch` (2025).** `import litert_torch` (driver falls back to `ai_edge_torch` for old sessions). `litert_torch.generative.quantize` is LLM-specific — for encoder INT8 weight-only quant use `ai_edge_quantizer.Quantizer` with `MIN_MAX_UNIFORM_QUANT`.
17. **`models/` is gitignored — model cards live in `docs/`** as `docs/preflight_memory_shared_vX.Y.Z_MODEL_CARD.md`.
18. **Pre-flight classifier MUST run on `com.google.ai.edge.litert:litert:2.x` — NOT `org.tensorflow:tensorflow-lite`, NOT Play Services TFLite.** Both classic runtimes silently produce broken output for our `ai-edge-quantizer` INT8 model (logits ~1500× reference, every query collapses to one class, no exception). `litert:2.x` is the Android port of Python's `ai-edge-litert` (`com.google.ai.edge.litert.CompiledModel`, different native libs). LiteRT-LM keeps its own runtime via `play-services-tflite-*`; both coexist. **Class-collision workaround:** litert AAR + transitive `tensorflow-lite-api` both bundle `org.tensorflow.lite.*` — exclude `org.tensorflow:tensorflow-lite-api` via `configurations.matching {…}.configureEach { exclude(...) }` in both `:shared` and `:androidApp`.

Embedder uses the same runtime (#18) + same vocab (`assets/vocab.txt` byte-identical to bert-base-uncased's); both engines share one tokenizer singleton. GPU delegate refuses both graphs (`BROADCAST_TO`/`EMBEDDING_LOOKUP`/`CAST INT64→FLOAT32` unsupported); CPU XNNPACK only. Classifier p95 113 ms, embedder p95 41 ms.

### Build, Android platform, framework

5. **`:shared` uses `com.android.kotlin.multiplatform.library`**, not `com.android.library` + `kotlin.multiplatform` (AGP 9 forbids it). Android config goes inside `kotlin { android { } }`.
6. **WorkManager's `SystemForegroundService` needs `foregroundServiceType` merged in the manifest** (`tools:node="merge"`), else any `setForeground(...)` worker crashes (`foregroundServiceType 0x1 is not a subset of 0x0`). Currently `dataSync` for the model download.
7. **`POST_NOTIFICATIONS` must be requested at runtime on Android 13+** (manifest alone is insufficient). `MainActivity.ensureNotificationPermission` on first launch.
19. **AGP 9 KMP source-set DSL rejects the closure form for catalog `Provider` deps.** `implementation(libs.foo) { exclude(...) }` inside `kotlin { sourceSets {…} }` fails — use configuration-level `configurations.matching { … }.configureEach { exclude(...) }` outside `kotlin {}`.
20. **SQLDelight `.sqm` files go alongside `.sq` in the same package dir** (NOT a `migrations/` subdir). With `N.sqm` present, `Schema.version` = `N+1`. `verifyMigrations = true` needs a committed `.db` snapshot per prior version in `src/commonMain/sqldelight/databases/`. **New-migration dance:** (a) generate `<currentVersion>.db` FIRST via `./gradlew :shared:generateCommonMainLocalAgentDatabaseSchema` while `.sq` is still old; (b) write `.sqm`; (c) update `.sq`; (d) rebuild. `ALTER TABLE ADD COLUMN` appends, so new columns MUST be declared at the END of `CREATE TABLE` or `verifyMigrations` flags column-order drift.
21. **Hilt `@Provides` methods do NOT auto-fill default constructor params.** A `@Provides`-constructed class with `@Inject constructor(…, dep: Foo = NoOpFoo)` MUST pass the real `Foo` or the default silently no-ops in production. (M6 Phase C, `TelemetryCounters`.)
22. **For "do X whenever screen Y is visible" use `LifecycleResumeEffect(key)`, NOT `LaunchedEffect(key)`** (the latter misses background→foreground, route key unchanged). Pair with `onPauseOrDispose { job?.cancel() }`. Load-bearing for aux (classifier+embedder) eager warm-up on Chat re-entry. Gemma is NOT eagerly warmed (PR #25 reversed M6 Phase B) — loads on first `InferenceSessionManager.generate()`.
23. **Firebase SDK deps live in `:androidApp`, NOT `:shared/androidMain`.** Define abstractions (`AnalyticsSink`, `SafeCrashReporter`) in `:shared/commonMain`; implement in `:androidApp`.

### Privacy, telemetry, redaction (M6)

24. **Firebase Crashlytics has NO `beforeSend` egress hook.** Redaction lives at every callsite behind the `SafeCrashReporter` facade; direct `FirebaseCrashlytics…recordException(...)` is a contract violation. The facade wraps in `RedactedThrowable` (keeps stack+class, scrubs message). **Never put user text in exception messages/breadcrumbs.**
25. **Crashlytics non-fatals batch until next launch.** `sendUnsentReports()` forces upload (`SafeCrashReporter.flushPending()`). Dedupes by exception class + top-of-stack.
26. **Compose `liveRegion = Polite` on growing/streaming text re-reads the whole string each update.** Do NOT put `liveRegion` on the streaming bubble. For one-shot announcements use `LocalView.current.announceForAccessibility(text)` from a `LaunchedEffect(messages.size)` firing only on growth.
27. **Counter telemetry is a separate channel from text-aware loggers.** Memory pipeline classes inject BOTH a `logger: (String) -> Unit` (counts/IDs/accelerator names — logcat-safe) AND a `TelemetryCounters` (off-device). DO NOT bridge logger → telemetry. `TelemetryPayloadBuilderTest` has a canary asserting a seeded marker never appears in any payload.
28. **Logcat tags for production loggers come from the DI module, not the class** (e.g. preflight lines tag `ClassifierModule`). Diagnostic filter: `adb logcat -s EagerWarmUp:I InferenceSessionManager:I TelemetryWorker:I ChatViewModel:I ClassifierModule:I VerticalSearch:I BraveApi:I MemoryRetriever:I MemoryExtractor:I MemoryBackupController:I AndroidRuntime:E`. `BraveApi` logs the outgoing `q` (+ chunked raw response for `/llm/context`) — diagnostic only. Per #27 `MemoryRetriever` logs only on the error path; silence is normal.

### Search verticals

Routing maps each `SearchSubtype` to one adapter via `VerticalSearchDispatcherFactory` (`adapters[subtype] ?: generalAdapter`). Endpoint per vertical differs (#37). Sources/defaults live in `search_defaults.json`; the Settings Add-source dialog uses `defaultKindFor`.

30. **No STOCKS subtype** — single-instrument quotes are part of FINANCE (one `site:` query answers without a ticker round-trip). `SearchSubtypeDetector` has no `STOCKS_PATTERN` (folded into `FINANCE_PATTERN`); no STOCKS `when` branches. See #33.
31. **SPORTS and FINANCE use the Brave `site:` path, NOT RSS** (RSS only carries recent headlines). `VerticalSearchDispatcherFactory` wires SPORTS → `BraveSiteFilterAdapter(subtype=…)`, FINANCE → `FinanceQuoteAdapter` (+ `BraveSiteFilterAdapter` fallback, #33). `FeedAdapter` (RSS) is live for **WEATHER only** (#32). **SPORTS & FINANCE single-source/single-chip:** one domain per country, `maxDomains=1, maxCitations=1`; `BraveSiteFilterAdapter` trims only the post-cache `payload.sources` (UI chips) via `SearchPostProcessor.limitCitations`. **`payload.json` (model's `[SEARCH CONTEXT]`) keeps Brave's full top-N** — capping starves the model. A `BRAVE_SITE_FILTER` source with zero domains issues an *unfiltered full-web* search (returns Success). **NEWS** is the exception → `NewsKindRoutingAdapter`, fanning the NEWS site list by kind (RSS/DWML/HTML/JSON → `FeedAdapter(subtype=NEWS)`, `BRAVE_SITE_FILTER` → `BraveSiteFilterAdapter`) and merging. **Anti-pollution:** the Brave side runs ONLY when ≥1 `BRAVE_SITE_FILTER` source exists (else unfiltered-Success contaminates a feed-only list). When both run, chips concat/dedup/cap at 10, payloads wrap `{"subtype":"news","query":…,"feeds":…,"web":…}`. NEWS caps citations at 10, ships two US defaults (`apnews.com` + `reuters.com`).
32. **WEATHER renders deterministically and is NEVER sent to the LLM** (Gemma mangles numbers, can't read a feed, doesn't know location). Fetches a national source — Environment Canada RSS (CA) or `forecast.weather.gov` DWML (US, `SourceKind.DWML` + `DwmlParser`) — and `WeatherResponseFormatter` builds the bubble. **Location resolved at query time, not onboarding.** Onboarding captures **country only** (`OnboardingViewModel.saveLocation(country)`, seeds defaults). Per-query order in `AgentLoop`: (a) city+region from the query via `WeatherLocationResolver` against `LocationCatalog`; else (b) a saved location memory; else (c) a deterministic prompt for city + region. Resolved city's country selects the source via `DefaultSiteResolver`. **WEATHER force-fires outside the classifier:** when the query matches the tight whole-message `BARE_WEATHER_PATTERN`, OR it names a resolvable catalog city AND carries explicit weather vocabulary (`SearchSubtypeDetector.mentionsWeather`) AND is **not** a past-tense question (`RelativeTemporalDetector.matchesPast`) — PR #89. The weather-keyword + not-past gate stops a bare city mention ("what is the history of london") and historical weather ("what was the weather like in London last year") from hijacking the live-forecast path. **Past-tense weather also downgrades inside the FireSearch handler:** a query that still reaches `AgentLoop` as subtype WEATHER via the pre-flight temporal force-fire (#38) is re-routed to GENERAL (`effectiveSubtype`) so it runs a normal web search + LLM synthesis instead of the live-forecast vertical. Saved-location fallback (b) is reachable ONLY via the bare pattern. **Ambiguous city → prompt, never assume (PR #89):** for a query-named city, `WeatherLocationResolver.resolveDetailed` returns `Unique`/`Ambiguous`/`None` and (unlike `resolve`) does NOT use the onboarded country to break ties. When the matched name maps to >1 distinct catalog place with no region/country qualifier in the text (London ON vs London England — **both in the catalog**; Sydney NS/NSW; the Springfields), `AgentLoop` disambiguates from a saved-location memory (`samePlaceAs`), else emits `emitWeatherDisambiguationPrompt` listing the specific options ("Did you mean London, Ontario (Canada) or London, England (United Kingdom)?"). An explicit region (name/code) or country (full name / alias `uk`/`britain`/`usa` / code) in the query → `Unique`, no prompt. When the city came from the user's words, `AgentLoop` emits `Done.locationToRemember` → consent card (`MemoryExtractor.proposeLocationMemory`, deduped); a memory-disambiguated pick is NOT re-proposed. `locations.json` covers CA + 50 US states + DC **plus GB and AU** (so famous international homonyms disambiguate); a city with a single catalog entry still resolves without a prompt even if a non-catalog homonym exists.
33. **Single-instrument FINANCE quotes render as a deterministic card via stockanalysis.com, bypassing the LLM.** `FinanceQuoteAdapter(fallback = BraveSiteFilterAdapter)`: (1) Brave `site:finance.yahoo.com` resolves the ticker from the URL's `/quote/<TICKER>/`; (2) GET `stockanalysis.com/stocks/<ticker>/` → `StockAnalysisParser` reads the minified `quote:{p,c,cp,h,l,v,h52,l52,…}` + `marketCap`/`peRatio`; (3) `StockResponseFormatter` renders a `"subtype":"stock_quote"` card. On ANY miss returns the Brave snippet unchanged → LLM fallback. **NOT cached** (price goes stale). `AgentLoop` has a FINANCE direct-render block after WEATHER. Needs `/web/search`, not `/llm/context` (#37).
35. **SPORTS runs Brave's LLM Context endpoint (`/res/v1/llm/context`), not `/web/search`** — pre-extracted page content (prose/tables) instead of ≤200-char snippets. `BraveSiteFilterAdapter` pinned to one domain but points at a second `SearchService` (`@SportsSearch`, `KtorBraveLlmContextClient`); `grounding.generic[]` → `LlmContextPostProcessor` → same `FormattedSearchPayload`. **Load-bearing tuning** (Gemma E2B mis-transcribes digits, "114"→"1114"): (a) `maxUrls=1`; (b) `LlmContextPostProcessor` keeps prose chunks unchanged and, for a chunk opening with `{`/`[` (structured JSON), **flattens the table shape `{ "title"|"caption": …, "table": [ {col: val}, … ] }` to one prose line per row** (`flattenTableJson` — caption prefixed, values joined ` — `, generic over column keys) so the model never sees raw JSON braces yet the content survives; **also rescues a schema.org Article-style object `{ "headline"/"name": …, "description": … }` to `headline — description`** (`flattenArticleJson`, PR #34 — Brave's **NEWS** llm/context returns each article as exactly this JSON object, so without it every NEWS snippet was dropped → `no usable results` Error on e.g. "what is the latest news today"); only genuinely text-less structured JSON (top-level array, table-of-arrays, VideoObject, scalar) is still dropped. Rescuing table JSON fixes the "all snippets are JSON → blank snippet → source filtered out → `no usable results` Error" failure (PR — e.g. "major sporting events June 2026"); (c) search-grounded turns decode greedy + drop history (#36). A deterministic score-card route is the only guarantee of correct digits — deferred (brittle for unstructured prose). `cacheNamespace="sports:"` so a `/web/search` payload can't serve a SPORTS turn.
38. **Relative-temporal queries force-fire search inside `PreflightRouter`** (NOT `AgentLoop` like WEATHER #32 — temporal force-fire WANTS the rewriter + subtype detection). Classifier under-fires on now-relative queries. `RelativeTemporalDetector` (`search/`) matches PAST/PRESENT/FUTURE relative phrases (topic-agnostic), widening the gate to `pSearch > highBand || temporalDetector.matches(query)`. `QueryRewriter.dateTimeRules` resolves `last/this/next` + `tomorrow` to concrete dates; unresolved phrases ("recently") pass through. **Excludes absolute dates** ("in 2019") and bare "now". Tracked via `preflight_temporal_force_total`. Logcat: ` forced=temporal`.
43. **An explicit "web search …" command at the START of a query force-fires search inside `PreflightRouter`** (like #38) — the user's deterministic escape hatch. `ExplicitSearchDetector` (`search/`) matches an **anchored leading** command — `web search …`, `search the web/online (for) …` (web-only; NOT `google`/`look up`). Anchored-at-start guards false positives ("how do web search engines work" doesn't fire). Widens the gate with `forceExplicit`. **Command words are stripped** (`stripPrefix`) before classifier/rewriter/subtype, but **vertical routing is kept**. **Fires even when the rewriter aborts** (the user asked). `SearchDisabled` still runs first. Tracked via `preflight_explicit_search_force_total` (explicit wins over temporal). Logcat: ` forced=explicit`. Default-constructed in `PreflightRouter`.

### Prompt assembly

34. **The `[SEARCH CONTEXT]` block + `PREFLIGHT_NOTICE` ride on the CURRENT user turn, NOT the system instruction.** `PromptAssembler.appendSearchContext` appends to the tail `HistoryMessage`; `buildSystemInstruction` no longer takes `searchContext`. **Don't move them back into the system prompt** — that reintroduces the disable-then-enable-search bug (a 2B model anchors on its own recent refusal adjacent to the generation point and ignores far-front evidence). Canonical RAG placement (most-recent) makes fresh results win. Defensive: if the tail isn't a `USER` turn, `appendSearchContext` leaves history unchanged. (Prior history is dropped on search turns anyway, #36.)
36. **Search-grounded turns drop prior history AND decode near-greedy.** Both gate on a non-blank `[SEARCH CONTEXT]` (GENERAL/NEWS/SPORTS; WEATHER/FINANCE render deterministically, never reach the LLM). (a) **History scoping:** `assembleStructured` calls `scopeToCurrentTurn`, keeping only the trailing USER turn for THIS generation; `AgentLoop` still persists full history. (b) **Greedy:** `AgentLoop` sets `GenerationRequest.sampling = SamplingParams.GREEDY` (`topK=1`, `temperature=1.0` — **NOT 0.0**, which risks divide-by-zero / stochastic-fallback; `topK=1` is the real lever). Reduces but does NOT eliminate digit errors (#35). Log: `historyTurns=N sampling=greedy(topK=1,temp=1.0)|default`.
37. **GENERAL/NEWS/SPORTS run `/llm/context` (each its own URL budget + cache namespace via a per-vertical `SearchService`); FINANCE deliberately stays on `/web/search`.** `KtorBraveLlmContextClient` ctor `maxUrls`/`maxTokens`/`maxSnippetsPerUrl` (default 1/1800/6):
    - **GENERAL** → default `SearchService`, `maxUrls=3`, `"ctx:"` (unpinned, >1 URL helps).
    - **NEWS** → `@NewsSearch`, `maxUrls=10`, `"news:"`; `BraveSiteFilterAdapter` `maxCitations=10`, `maxDomains=3`. Wrapped in `NewsKindRoutingAdapter` (#31).
    - **SPORTS** → `@SportsSearch`, `maxUrls=1`, `"sports:"` (#35).
    - **FINANCE** → `@FinanceSearch`, `KtorBraveSearchClient` (`/web/search`), `"fin:"`. **Must NOT use `/llm/context`** — its URLs lack `/quote/`, so `FinanceQuoteAdapter.resolveTicker` (#33) silently fails. `KtorBraveSearchClient` + `SearchPostProcessor.format` are **live, not deprecated**.
    Distinct cache namespaces keep response shapes + URL budgets from mixing in the shared `SearchCacheDao` (`"ctx:"` also invalidates pre-existing un-namespaced rows). `VerticalSearchModule` injects the four qualified `SearchService`s. `BraveSiteFilterAdapter`/`LlmContextPostProcessor`/`limitCitations`/greedy+scoping are endpoint-agnostic.

### Vision / image input

39. **Image input is gated at engine init, sent on the current turn only, and bypasses preflight/search.** Vision needs `EngineConfig.visionBackend` + `maxNumImages` at init (NOT per-request). `LiteRtInferenceEngine.newEngine` sets `visionBackend = Backend.GPU()` + `maxNumImages = 1` when `InferenceConfig.enableVision` (chat-load default flips it true; model keyed on `modelPath`, first loader's config wins). Image is a **downscaled JPEG `ByteArray`** on `…imageBytes` fields → `Message.user(Contents.of(Content.ImageBytes(bytes), Content.Text(text)))`. **`Content.ImageBytes`, NOT `Content.ImageFile`** (Photo Picker returns a `content://` Uri). **Model sees trailing-turn-only:** engine's history path is text-only; `PromptAssembler` strips `imageBytes` from non-trailing turns. **Display persists** the JPEG as a `messages.image_bytes` BLOB (migration `5.sqm`, v5→v6); `UserBubble` decodes on demand via `produceState`; cleanup free via FK `ON DELETE CASCADE`. `AgentLoop` still writes a text-only `ChatMessage.User` to in-memory history. **An image turn skips deterministic short-circuits AND preflight/search** (`hasImage` gate → `FallThrough(MiddleBand)`, `forceWeather` suppressed, warm sampling). UI: Android Photo Picker (`PickVisualMedia`, no permission) → `ImagePreprocessor` decodes + downscales longest edge to ~768 px (`ImageDecoder`, applies EXIF) + JPEG-encodes; no Coil. `StubInferenceEngine` echoes `[stub] received image of N bytes`. **Pending-attachment preview must match the sent bubble (PR #69):** the `ChatScreen` thumbnail uses `ContentScale.Fit` + `widthIn(max = 240.dp)` (NOT `Crop` at `size(56.dp)`); commonMain → both platforms. **The staged chip ALSO carries `heightIn(max = 160.dp)` (PR #36)** — without a height bound a portrait (taller-than-wide) photo renders ~320–420 dp tall and, as a fixed-height sibling of the text field in the non-scrolling input `Column`, pushes the input off-screen once `imePadding()` shrinks the column for the keyboard. The sent-bubble copy in `UserBubble` stays unbounded (it lives in the scrollable message list, no keyboard collision). **Benign Pixel 7 noise:** native `litert: No dispatch library found / Failed to initialize Dispatch API` is LiteRT-LM probing for an absent NPU delegate (falls back to GPU, succeeds). Mute: `adb shell setprop log.tag.litert SILENT` or filter `litert:S`.
40. **LiteRT-LM and the classifier runtime ship colliding `libLiteRt.so` — pin BOTH to `litert:2.1.5`, force litert's copy to win via project-local jniLibs, never bump independently.** LiteRT-LM 0.11.0+ ships standalone `libLiteRt.so` + `libLiteRtClGlAccelerator.so` sharing names with `com.google.ai.edge.litert`'s libs; Android packages one `.so` per name and the builds are never byte-identical. **NOT interchangeable:** (a) LiteRT-LM's `libLiteRt.so` lacks `Java_com_google_ai_edge_litert_*` JNI symbols → classifier crashes `UnsatisfiedLinkError: Environment.nativeCreate`; (b) `litert:2.1.4`'s libs SIGSEGV `liblitertlm_jni.so` 0.12.0 in GPU init. **`litert:2.1.5` is the one combination serving both** (its `libLiteRt.so` is a superset that also drives `litertlm_jni`). **`pickFirst` alone does NOT reliably pick litert's copy** (merge order non-deterministic). **Fix:** the `extractLitertJni` Copy task in `androidApp/build.gradle.kts` pulls litert's two natives from its own AAR (resolved version, arm64-v8a) into a generated jniLibs source dir registered via `android.sourceSets.getByName("main").jniLibs.directories.add(<path>)` (PR #24 — was the now-deprecated `jniLibs.srcDir(File)`; AGP 9 removed the single-arg form in favour of the `directories` mutable set, which takes a path **String**, not a `File`) — project-local jniLibs always beat dependency libs in `pickFirst` — tracking the resolved version. Keep the `packaging { jniLibs { pickFirsts += ["**/libLiteRt.so", "**/libLiteRtClGlAccelerator.so"] } }` block. **Compile-only `assembleDebug` will NOT catch a regression here** — re-verify on-device, or check the merged `libLiteRt.so` exports `Environment_nativeCreate` (`nm -D`). `litertlm` + `aiEdgeLitert` versions are **coupled**.

### Markdown / LaTeX rendering

41. **Markdown + LaTeX render ONLY for answers the model composes freely; search-grounded and deterministic turns render plain — gated by a persisted `renderMarkdown` flag.** Finalized assistant text renders via Markwon + `io.noties.markwon:ext-latex` (jlatexmath — native canvas, offline, **NO WebView**) in an `AndroidView` TextView (`MarkdownMathText`); the streaming partial keeps plain Compose `Text` (per-token reparse would jank). `renderMarkdown` lives on `ChatMessage.Assistant`/`UiMessage.Assistant`, persisted as `messages.render_markdown` (migration `6.sqm` → v7), **false** for WEATHER/FINANCE cards, the clock/my-list/weather-prompt/memory-ack handlers. **PR #103 — ALL LLM-composed turns render markdown, including search-grounded ones.** Originally search-grounded turns (`searchContextBlock != null || citationsForTurn.isNotEmpty()`) rendered plain, but search answers routinely emit `*` lists / `**bold**` / source links that show as raw symbols when plain, and subtype-gating (NEWS/SPORTS only) missed sports/news queries the classifier routes to GENERAL. So `AgentLoop` now sets `renderMarkdown = true` unconditionally at the final-message construction: **every path reaching it is LLM-composed** (free prose/math OR a `[SEARCH CONTEXT]`-grounded answer). The deterministic cards that must stay plain (WEATHER/FINANCE/clock/my-list/memory) **early-return with their own `renderMarkdown=false`** and never reach that point — so stock-quote cards stay plain by design. **ext-latex 4.6.2 matches `$$…$$` only**; the model emits single-`$`, so `LatexNormalizer.normalize` rewrites `$…$`/`\(…\)`/`\[…\]` → `$$…$$` with a math-vs-currency heuristic. **Currency guard (PR #103):** `looksLikeMath` returns false when the inner `$…$` content starts with a digit (`$5`, `$219.31`) — keeps financial prose like `$5 (a 10% gain) to $8` literal; the structural-LaTeX-token check (`\ ^ _ { } =`) stays FIRST so coefficient-led math (`$4k^2 = 8$`) still renders. Search turns' two markdown hazards are both handled: single-newline collapse by `SoftBreakAddsNewLinePlugin`, currency `$` by the guard. `MarkwonInlineParserPlugin` + `SoftBreakAddsNewLinePlugin` registered. TextView mirrors `bodyMedium`. All assistant turns share one `AssistantBubble`; `renderMarkdown` swaps only the inner content. **Dev-build identity:** `BuildConfig.GIT_DESCRIBE` shows in the About dialog (versionCode = HEAD's commit timestamp) — a stale install is obvious.

### Voice I/O — dictation (STT) + read-aloud (TTS)

**Full as-built record + the rationale for both invariants below: `docs/VOICE_IO.md`.**

42. **Android voice I/O is entirely `:androidApp` (no `:shared` seam), behind `ChatSpeaker`/`SpeechDictation`.** Read-aloud fires ONLY at `AgentEvent.Done`; the working-on-it ack + heartbeat gate on `AgentEvent.GenerationStarted` (NOT `send()`). Mic is a continuous, session-only toggle (defaults OFF, not persisted; self-restarting single-shot `SpeechRecognizer`, needs `RECORD_AUDIO` + `<queries>` `RecognitionService`). Streaming dictation (PR #67): `Dictation.partials` is display-only via a `committedInput` anchor; **`VoiceCommand.match` runs ONLY on `results`** and a voice SEND sends `committedInput`, never `input`. Echo suppression = `ttsSpeaking` + 2.5 s grace tail. Android TTS uses the OS voice settings (no in-app picker). See VOICE_IO.md.
48. **Desktop read-aloud (PR #66) shells out per-OS, and stopping is OS-specific: `DesktopTtsSpeaker.stop()`'s `process.destroy()` silences macOS/Windows but NOT Linux `spd-say` (a client; the speech-dispatcher daemon outlives it), so `stop()` also issues `spd-say --cancel`.** Desktop voice IS configurable (engine/voice/rate on the concrete `DesktopTtsPreferences`, OFF the shared `TtsPreferences` interface — same split as #45; `expect/actual DesktopVoiceSection()`). The quality win is the bundled offline **Piper** neural engine (`PiperSpeechSynthesizer`; `PiperBinaryStore`/`PiperVoiceStore` download like the LLM/Vosk models; raw S16LE streamed to `javax.sound.sampled.SourceDataLine`, stops instantly). See VOICE_IO.md.

### Desktop appearance & window (CMP, PR #61)

45. **Desktop UI "zoom" scales `LocalDensity.density`, NOT `fontScale` — different levers.** `AppThemeScaffold` (shared `:ui`) takes `densityScale: Float = 1f` (default ⇒ Android untouched) and applies `Density(base.density * densityScale, base.fontScale * fontScale)`. Density scales `dp`+`sp` (whole UI: icons, padding, forms, text); `fontScale` is the text-only Settings slider. **For "everything too small" use density, never fontScale.** Desktop `Window(onKeyEvent=…)` drives it via **Ctrl/Cmd `+`/`-`/`0`** (modifier-gated so it never eats typed chars); persisted desktop-only on `DesktopThemePreferences.uiZoom*` (`theme_prefs.json`, `UiZoom` bounds 0.8–2.0) — OFF the shared `ThemePreferences` interface. `Main.kt` reads the **concrete** `DesktopThemePreferences` from Koin.
46. **Desktop theme is monochrome, not the Android green; `ThemeMode.System`/Auto is unreliable on Linux.** Desktop `Theme.kt` defines `LightMonochromeColorScheme` (white surfaces, black trim) + `DarkMonochromeColorScheme` (near-black `#121212`, white trim), with `surfaceTint = Color.Transparent` to kill M3 tonal-elevation overlays; `error` stays red. Android keeps Material You / the green fallback. **`isSystemInDarkTheme()` on desktop is Skiko-backed — works on macOS/Windows, but on Linux commonly reports light with no live update (JetBrains CMP-6028).** No Linux detector wired; use the manual Light/Dark selector there. Status dots (memory/connection green-yellow-red in `:ui`) are semantic, not theming — leave them.
47. **Desktop window geometry is persisted; chat bubbles size to window width.** `DesktopWindowPreferences` (`window_prefs.json`) saves size/position/maximized; `Main.kt` seeds `rememberWindowState` + saves on change via `snapshotFlow` + `collectLatest` debounce (width/height saved only while *floating* so maximizing doesn't clobber the restore size; position only when absolute). Chat bubbles: `ChatScreen.kt` wraps the list in `BoxWithConstraints` — `<600.dp` keeps the mobile 320 dp cap, wider uses `(maxWidth*0.72f).coerceAtLeast(480.dp)` (PR #32 — was `.coerceIn(480.dp,760.dp)`; the 760 dp upper cap was removed so desktop bubbles keep growing with the window); threaded as `maxWidth: Dp` into `UserBubble`/`AssistantBubble`/`StreamingAssistantBubble`.

### Jobs (PR #70)

Time-triggered work that runs **only on the desktop agent** (cron or one-shot), with a mobile remote view (the mobile Jobs icon shows **only when paired** since PR #80 — see #56). **Full as-built design + the rationale for all four invariants below: `docs/JOBS_PLAN.md` §0.**

49. **A job is `command` + `prompt`, always a desktop subprocess; each run continues the job's ONE conversation thread.** `JobExecutor` runs `command` with `prompt` (the UI **"Command Argument"** field) as a bound positional shell arg (injection-safe), appends `user`/`assistant` to the job's `last_run_conversation_id`. **The `user` turn is the job NAME, not `prompt`** (PR #84 — the name reads sensibly in the thread; `prompt` is the command's input, not chat text); `assistant` = the subprocess output (`renderMarkdown=true` since PR #82). `JobScheduler`/`JobExecutor`/`JobService` (commonMain `JobAdmin` seam; **null on mobile → read-only *definitions*, but see #50 run-now**) + `cron-utils` are **desktopMain-only**. **Run start writes `lastRunStatus=RUNNING` to the denormalized (synced) job row** so both platforms show a working spinner mid-run; the terminal `recordLastRun` overwrites it (PR #84). See JOBS_PLAN §0.
50. **Job trust boundary = the injected `JobSyncPolicy`, fail-closed; the `*FromPeer` write needs the `onJobPausedFromPeer` seam to re-drive the scheduler.** Create/edit/delete are desktop-only; a mobile peer may only toggle `paused` **and trigger run-now** (PR #84). `DesktopJobSyncPolicy` drops remote inserts + tombstones; the raw `updatePausedFromPeer` query fires no `LocalChangeBus`, so `applyFromPeer` must call `onJobPausedFromPeer` → `JobService.reactToPausedChange`. `jobs` syncs LWW+tombstone with denormalized last-run; `job_runs` is desktop-local, NOT synced. **Mobile run-now does NOT ride sync** — it's an imperative **`RUN_JOB` `LinkMethod`** over the relay (job id in `query["id"]`): mobile `RemoteJobRunner`/`RelayRemoteJobRunner` (bound mobile-only) → `DesktopLinkRequestHandler.runJob` seam → guard on `JobRepository.get(id)` → `JobService.runNow`. Online-only (needs the link UP); the run-now ▶ button renders under `canControl` (desktop always; mobile when relay UP), Edit/Delete stay `isAdmin`-only. See JOBS_PLAN §0.
51. **`SqlDelightJobRepository.flow()` MUST be a reactive SQLDelight query (`selectAllJobs().asFlow().mapToList`), NOT a seeded `StateFlow`.** Synced rows are written via raw `*FromPeer` queries that bypass the repo, so the tasks/my-list `MutableStateFlow` pattern goes stale (a synced job is invisible until restart). Local writes still call `LocalChangeBus.notifyChanged()`. **New-desktop job wipe (PR #39):** pairing a *different* desktop (stable X25519 `desktopPublicKey` changed) fires `onPairedDifferentDesktop` in `AndroidRelayBytePipeFactory` → `JobRepository.wipeLocal()` (hard DELETE jobs+runs, no tombstone/echo) + `SyncWatermarkStore.set(0)` so the new desktop's state re-pulls. **A fresh re-install on the SAME desktop keeps the pubkey, so that never fires** → a mobile-only manual **"re-sync jobs"** button (top-right of `JobsScreen`, `JobsViewModel.canResync`/`resyncJobs`) runs the same wipe+reset **plus** `SyncController.requestSync()` (the only public reconcile trigger) for an immediate re-pull; composed by `jobResync(...)` (`shared/.../job/JobResync.kt`), bound `single<JobResync>` mobile-only (null→hidden on desktop). See JOBS_PLAN §0.
52. **No Material3 `DatePicker` on desktop — its kotlinx-datetime calendar model throws `NoSuchMethodError` (`KotlinxDatetimeCalendarModel.getToday`).** The Jobs schedule form reuses the mobile clock UI (**Repeat**=alarm flow → 5-field cron, **Once**=timer flow → one-shot `fireAt`); desktop swaps the analog clock for `TimeInput`. The monochrome theme also had to define the M3 `surfaceContainer*` roles or `AlertDialog`/menus tint purple. Mobile `RuleSettingsIcon` is a hand-built `ImageVector`. See JOBS_PLAN §0.
58. **Mobile job-completion signals (PR #85) are mobile-only, foreground-only, and ride the existing reactive `JobRepository.flow()` — no new sync path.** Jobs finish on the desktop and reach the phone via sync (#49–51); two cues fire off the flow, both treating a run as *noteworthy* only when `lastRunStatus ∈ {SUCCEEDED, FAILED}` (`Job.noteworthyRunAtEpochMs`; RUNNING/CANCELLED signal nothing): (a) a **chat-header count bubble** on the Jobs icon (`BadgedBox` + a tinted numbered `Badge`, the pre-#26 alarm/timer/my-list style — bubble matches `LocalContentColor`) driven by `JobBadge.unseenCount` (jobs with a noteworthy run newer than the persisted **seen** watermark), cleared by `JobsViewModel.markSeen()` from a `LaunchedEffect(jobs)` in `JobsScreen`; (b) an **Android OS notification** via `JobCompletionNotifier` → `AndroidNotificationPresenter` — the **first binding** of the commonMain `NotificationPresenter` seam (new `NotificationKind.JOB`, `job_runs` channel, distinct success/error copy), deduped by a persisted **notified** watermark with a **baseline-suppress on the first emission per process** (so the initial sync backfill doesn't storm). Both watermarks: `JobNotificationPrefs` (Android `SharedPreferences` `job_notify_prefs`, mirrors `SyncWatermarkStore`). **`JobBadge` binds ONLY in `androidModule`** → null on desktop (`JobsViewModel.badge` + `ChatScreen` resolve via `getOrNull`, so adding the param doesn't break the desktop graph). **PR #93 UPDATE — the OS-notification cue is no longer mobile-only:** `JobCompletionNotifier` + `JobNotificationPrefs` now ALSO bind on the desktop (the machine that *runs* the job surfaces its own result), so a finished desktop job fires a notification via the desktop `NotificationPresenter` (`notify-send` on Linux / tray toast on macOS/Windows). See #60. Only the **header badge** stays mobile-only. The notifier is started/stopped by `LocalAgentApplication`'s `WatchdogForegroundGate` (foreground-only, matching mobile sync). **No new permission** (POST_NOTIFICATIONS already handled, #7) and **no foreground service** (ordinary notifications). Adding `NotificationKind.JOB` makes the desktop `AwtTray` `when` non-exhaustive — keep it covered. **Jobs list sort:** `JobsViewModel.jobs` sorts by `lastRunAtEpochMs` descending (never-run → bottom; stable so a new unrun job sinks to the very bottom) — shared VM, so both platforms. See JOBS_PLAN §0.

59. **`run job <name> <keyword(s)>` is an inline chat force-fire (PR #88) that runs a desktop job and renders its output DIRECTLY as the assistant turn (markdown, NO LLM) in the CURRENT thread — like the deterministic WEATHER/FINANCE cards (#32/#33), NOT the job's own conversation/run history (#49).** `RunJobDetector` (anchored `^run job\b`, `job\b` rejects "run jobs") + `RunJobResolver` (longest job-name **token-prefix** match against `JobRepository.snapshot()`, remainder = keyword(s), **empty → the job's saved `prompt`**) run in `AgentLoop` **above** pre-flight (gated on `!hasImage` + both job deps bound), because name resolution needs the synced job list and runs a subprocess — wrong layer for the router. On a match it emits `AgentEvent.JobStarted(name)` (UI `SearchStatus.RunningJob` chip, cleared on `Done`), runs the job via the **`InlineJobRunner`** seam, then `emitJobOutput` sends a deterministic `Done` with the raw output as `ChatMessage.Assistant(renderMarkdown=true)` and returns — **the LLM is never invoked** (the raw markdown/tables/links ARE the answer; a 2B model would mangle them). **`renderMarkdown=true`** (links/tables format, PR #82) — the OPPOSITE of search-grounded plain (#41); no `TokenChunk` (avoids a raw-markdown flash on the plain streaming bubble). The output persists in the thread, so the user can ask the LLM follow-ups. Not-found / failure short-circuit deterministically (plain text). **The seam is the whole platform split:** desktop `LocalInlineJobRunner` → **`JobExecutor.runCapture`** (subprocess with the keyword(s) overriding `prompt`, **writes NO conversation/run/last-run rows** — side-effect-free, unlike `execute()`); mobile `RelayInlineJobRunner` → new **streaming** `RUN_JOB_INLINE` relay method (`query{id,keywords}`, desktop `DesktopLinkRequestHandler.runJobInline` emits one `Data{"ok",output}`+`End(200)`, `End(404)`=unknown id). **Streaming (not unary) is load-bearing for Cancel:** the existing chat Cancel button (already shown while `isGenerating`) cancels the turn coroutine → desktop `runCapture` is **cancellation-aware** (poll-loop `ensureActive()` + `finally` `destroyForcibly`, mirrors #29) and over the relay a CANCEL frame cancels the desktop handler → kills the subprocess. Both bound via `getOrNull` in `AgentCoreModule` (null where jobs aren't bound → feature inert). See JOBS_PLAN §0.

### Cancel a running job (M5 PR)

69. **Cancelling a running job kills the process TREE and records CANCELLED; the run-now ▶ becomes a Stop ⏹ while `lastRunStatus == RUNNING`, on BOTH platforms.** Symmetric with run-now (#50), a separate imperative channel (NOT synced). Desktop is the authority: `JobService` keeps an in-flight registry keyed by job id, and BOTH `runNow` and the scheduler fire-path (`onJobFired`) now run the job in a **tracked, `CoroutineStart.LAZY`** coroutine (so a scheduled run is cancellable too). `JobAdmin.cancel(id): Boolean` cancels that coroutine — false when none is in flight. **`JobExecutor.runProcess` is now cancellation-aware** (poll loop + `coroutineContext.ensureActive()`, mirroring `runCapture`/#29/#59) so the cancel breaks the blocking `waitFor` promptly, and **`execute()` records the terminal CANCELLED row under `NonCancellable`** — the previous `runCatching { runProcess }` swallowed `CancellationException` and mislabelled a cancel as FAILED, so DON'T wrap a cancellable call in `runCatching` expecting a normal result. **`destroyTree(proc)`** snapshots `ProcessHandle.descendants()` and `destroyForcibly()`s the whole subtree **BEFORE** the parent (killing the parent first reparents/orphans grandchildren and loses the handles) — now used on **both cancel AND timeout**, in `runProcess` and `runCapture` (a bare `destroyForcibly` only killed the direct child). Best-effort: `descendants()` is a snapshot, so a child spawned during the kill window can survive. **Mobile cancels over the relay via a new imperative `CANCEL_JOB` `LinkMethod`** (unary, id in `query["id"]`): `RemoteJobRunner.cancel`/`RelayRemoteJobRunner` (mobile-only) → `DesktopLinkRequestHandler.cancelJob` seam → `JobService.cancel` (200 if cancelled, else 404), wired in `Main.kt`; online-only. UI: `JobsViewModel.cancel` routes to the local `JobAdmin` (desktop) or the relay runner (mobile, link UP); the Stop button uses `JOBS_CD_CANCEL_RUN` (EN+ES). See JOBS_PLAN §0.

### Desktop notification delivery (PR #93)

60. **Linux desktop notifications go through `notify-send`, NOT the AWT system tray; and job completions now notify ON the desktop too.** Two coupled changes:
    - **Delivery (Linux):** on modern GNOME/Wayland the AWT `SystemTray` is usually unsupported (`Main.kt`'s `traySupported` probe returns false), so the tray-toast presenter (`AwtTray.notificationPresenter`, `TrayIcon.displayMessage`) silently degraded to the stderr-logging `DesktopNotificationPresenter` — invisible. **`LinuxNotificationPresenter`** (`shared/desktopMain/notification/`) shells out to the freedesktop `notify-send` CLI (fire-and-forget `ProcessBuilder`, same best-effort pattern as `DesktopTtsSpeaker`/`spd-say`; missing CLI / no D-Bus → caught + logged, never crash). argv built in a pure `internal buildArgv` (unit-tested, no spawn); `ALARM`/`TIMER`/`ongoing` → `--urgency=critical` (persists, no auto-expire), `TASK`/`JOB`/`INFO` → `--urgency=normal`; **keep the `when` over `NotificationKind` exhaustive** (same as `AwtTray`). `dismiss()` is a no-op (no D-Bus `CloseNotification`), matching the tray. **Wiring:** `DesktopModule` sets the `MutableNotificationPresenter` **fallback** = `LinuxNotificationPresenter` on Linux else logging `DesktopNotificationPresenter` (covers GUI, tray-minimized, AND windowless-headless — `Main.kt` returns before `application{}` so it never `setDelegate`s); `Main.kt` gates `presenter.setDelegate(tray.notificationPresenter)` to **`!DesktopOs.isLinux`** so `notify-send` wins on Linux even if a tray exists (the tray's Show/Shut down menu still installs). `DesktopOs.isLinux` (`os.name` substring idiom, shared with `Main.kt`). **macOS/Windows are unchanged** (tray toasts — to be evaluated there later). `AwtTray.kt` untouched.
    - **Desktop job-completion notifications (updates #58):** `JobCompletionNotifier` was Android-only (jobs notified the *remote phone* via sync), so a finished **desktop** job produced no desktop notification. Now the commonMain `JobCompletionNotifier` + a new **`DesktopJobNotificationPrefs`** (`DesktopJsonStore` `job_notify_prefs.json`; seen-watermark inert — no desktop header badge; notified-watermark dedupes) bind in `DesktopModule`, and `Main.kt` starts the notifier on `appScope` right after job `rearmAll` (runs in GUI + headless; the baseline-suppress on the first flow emission stops the startup backfill from storming). So a desktop job's SUCCEEDED/FAILED now surfaces via the path above.

### Desktop deployment (PR #72)

53. **`LOCALAGENT_HEADLESS=1` runs the desktop agent as a startup service without opening the main window.** The full background runtime launches on `appScope` **before** the `application{}` block (headless only changes *window startup*): tray available → `windowVisible=false`; no tray → fully windowless on a `CountDownLatch` + SIGTERM/Ctrl-C hook mirroring `shutdown()` (no `exitProcess`). `AppIcon` is `lazy` so the windowless path touches no AWT/Skia. Single-instance file lock (`<app-data>/.instance.lock`) shared across modes. **Full detail + service templates (`desktopApp/packaging/`) + deploy guide: `docs/DESKTOP_PACKAGING.md` "Headless / standalone deployment".**

### Paid "anywhere access" — relay subscription (PR #74, scaffold; transport is a follow-up)

54. **Paid "anywhere access" lets the desktop buy a Secure Gateway relay subscription so the phone reaches it from anywhere (vs LAN-only #57). The PAYMENT half is in; the relay TRANSPORT is a deliberate follow-up.** Desktop-only feature behind the `subscription/` package (`:shared`); mobile/iOS bind **NoOp** (`SubscriptionPreferences`/`SubscriptionUiController`) — the phone never purchases. **Secret handling is load-bearing:** the gateway account secret lives in `SecureStorage` (`SecureStorageKeys.RELAY_ACCOUNT_SECRET`); the non-secret ids (account/subscription/license id + last-known status) live in `subscription_prefs.json` (`DesktopSubscriptionPreferences`). **Never store the secret in the DB/prefs.** **Onboarding = the gateway's claim-token flow:** `RelaySubscriptionService.startCheckout` mints a nonce + loopback redirect (`http://127.0.0.1:<DesktopLinkServer port>/subscribe/callback`) → `POST /v1/checkout/start` → opens Stripe Checkout via `DesktopUrlOpener`; the **`/subscribe/callback` route on the existing `DesktopLinkServer`** (browser-facing, unauthenticated, loopback-only) hands the one-time `claim_code` to `RelaySubscriptionService.handleClaimCode` → `POST /v1/accounts/claim`; a fallback poll on the nonce covers the webhook/browser race. **Every launch** `Main.kt` calls `RelaySubscriptionService.refresh()` (→ `GET /v1/subscription`) **only when an account exists**; offline-tolerant (keeps last-known). UI: the shared `SettingsScreen` "Local Agent Connection" desktop section flips copy to "…connect to this desktop **anywhere**…" + a **"Subscription Settings"** link when `subscription.isActive`, else **"Upgrade to anywhere connection"** (gated on `SubscriptionUiController.available` = desktop + configured). **"Subscription Settings"** opens the **Stripe Customer Portal**: `RelaySubscriptionService.openSubscriptionSettings` mints a fresh portal URL via the gateway (`RelayGatewayClient.billingPortalUrl` → `POST /v1/billing-portal`, account-secret auth) and opens it, falling back to a static `LOCALAGENT_SUBSCRIPTION_PORTAL_URL` only if that fails (the Stripe portal must be enabled in the dashboard). Gateway + relay URLs ship **production defaults baked into every debug/release build** (`RelaySubscriptionService.DEFAULT_GATEWAY_URL = https://auth.contextsolutions.com`, `DEFAULT_RELAY_WS_URL = wss://relay.contextsolutions.com/v1/connect`; PR #37), overridable at runtime via `LOCALAGENT_GATEWAY_URL` / `LOCALAGENT_RELAY_WS_URL` (not a Settings field). A custom `LOCALAGENT_GATEWAY_URL` with no relay override still derives the relay host from it (`https→wss`, `/v1/connect`) so local testing stays on one host; only with nothing set do the baked defaults apply. (Was empty-string ⇒ `isConfigured=false` ⇒ feature silently disabled before #37.) **Consumed-claim UX:** the desktop's fallback poll usually claims before the browser returns, so `/v1/checkout/return` finds the claim consumed and shows a success page (gateway-side, not "expired"). **SDK consumption:** `com.securegateway:java` via **`mavenLocal()`** (publish from `../secure-gateway/sdk`), wired into **`:shared/desktopMain` only** — JVM/libsodium; the Android SDK is deferred to the transport PR (lazysodium-android + the #40 native-lib collision). **The transport follow-up (B4 relay QR, C mobile relay pairing, T tunnel sync+inference) is now implemented — see #55.** **Gateway side = secure-gateway PR #6** (`POST /v1/checkout/start`, `GET /v1/checkout/return`, `POST /v1/accounts/claim`, `GET /v1/subscription`, `POST /v1/billing-portal`). See `docs/ANYWHERE_ACCESS_PLAN.md`.

55. **[Partially SUPERSEDED by #56 — PR #80 removed the LAN path; the relay is now the ONLY mobile↔desktop transport. The dual-path detail below is the as-built design from #75–77; read #56 for the current shape.]** **The relay transport tunnels the mobile↔desktop link (chat + sync) over the Secure Gateway E2EE relay when subscribed; the LAN path (#57) stays intact for non-subscribers + as the offline/revoked fallback (relay PR #75 / secure-gateway PR #7).** A single **`LinkTransport`** seam (`link/transport/`, commonMain) abstracts unary + server-stream of opaque strings; the existing wire payloads (OpenAI chat JSON, post-`data:` SSE chunks, `SyncBundle` JSON) ride **unchanged** inside the frames — only the transport swaps, so `DesktopLinkInferenceEngine` / `LinkSyncClient` (was `LinkSyncHttpClient`) / `SyncController` are transport-agnostic. **`LanLinkTransport`** = today's Ktor HTTP (refactored, byte-identical). **`RelayLinkTransport`** (mobile) frames RPC over a **`RelayBytePipe`** (the SDK's opaque `send`/`onMessage`) via **`FrameMultiplexer`** (client) ↔ **`FrameDispatcher`** (server): a JSON **`LinkFrame{v,id,kind,method,query,status,body}`** with `id` correlation, `REQUEST→RESPONSE` (unary) / `REQUEST→STREAM_DATA*→STREAM_END|STREAM_ERROR` (streaming) + `CANCEL`, per-frame peer-ack backpressure. **Desktop = relay request-server:** the route bodies live in ONE shared **`DesktopLinkRequestHandler : LinkRequestHandler`** called by BOTH the Ktor `DesktopLinkServer` and the relay `FrameDispatcher`; `Main.kt` runs the relay **concurrently** with the LAN server when `subscription.isActive` (mint relay QR → `awaitPairing` → `connect` → serve), publishing the relay QR (wins over the LAN `magent://` while subscribed). **`DesktopRelayHost`** (was the `RelayLinkTransport` stub) + `DesktopRelayBytePipe` wrap `DesktopClient`; `AndroidRelayBytePipe`/`AndroidRelayBytePipeFactory` wrap `MobileClient` (androidMain — SDK types stay out of commonMain, #23). **AccessMode** on `DesktopLinkConfig` (`accessMode`/`relayQrJson`, `isRelayConfigured`) is derived in `SettingsViewModel.applyScannedLink` (relay QR detected via the commonMain `RelayQrPayload` mirror, NOT the SDK type); `DefaultLinkTransportProvider` establishes the relay pipe in the background and `current()` returns it only while UP, else null → on-device fallback (re-decide pushed via `OllamaConnectionMonitor.requestReload`). **Mobile credential (security L2 — per-pair credential, secure-gateway PR #8 / SDK 0.2.4 / local-agent PR #20):** the phone has no subscription, but the desktop's account secret **no longer rides the QR**. The gateway mints a **per-pair credential** at pairing completion (and registers the mobile device from its public key, authorized by the pairing token), and the phone authenticates token issue/refresh + unpair with **that** (`CompletePairingResult.pairCredential`/`.mobileDeviceId`; `MobileConfig.pairCredential` restores it for reconnect). `DesktopClient.generatePairingQr` stopped injecting `QrPayload.accountSecret`; the consumer stopped persisting `RELAY_ACCOUNT_SECRET` from the QR and instead persists the per-pair credential inside the `RELAY_PAIRING_STATE` blob (`AndroidRelayBytePipeFactory`). The account secret stays entirely on the desktop (still in `RELAY_ACCOUNT_SECRET` for the desktop's own gateway auth). Account-secret fallback retained only against a legacy gateway. **Treat the relay `4004` close (REVOKED) as "subscription ended"** → `LinkConnectionState.DISABLED` → fall back. **On-device crypto shipped (secure-gateway PR #8 / local-agent PR #76):** the mobile SDK now publishes a real Android AAR (`com.securegateway:android` = the `:android-aar` `com.android.library` module on **lazysodium-android**); the plain-JVM `:android` is retained **unpublished** only so the hermetic cross-platform `:java:e2eTest` keeps running on the JVM. `:core/Crypto` types against the base `LazySodium` and resolves the concrete binding via a `ServiceLoader` `SodiumProvider` (JVM modules register `LazySodiumJava`; the AAR registers `LazySodiumAndroid`); `:core`'s lazysodium-java is `compileOnly` so it never reaches the Android classpath. `SodiumAndroid` is JNA-based, so the AAR excludes lazysodium-android's plain-jar `jna` and pulls `net.java.dev.jna:jna:5.17.0@aar` (ships `libjnidispatch.so`). **16 KB page-size:** lazysodium-android is pinned **5.2.0** (not 5.1.0) because its `libsodium.so` has 16 KB-aligned ELF LOAD segments (5.1.0 was 4 KB — fails Android's 16 KB check, required by Play for API 35+); 5.2.0 needs the `:android-aar` Kotlin plugin bumped to 2.1.x (it pulls androidx.core-ktx built against the Kotlin-2.1 stdlib — a separate plugin app from the JVM modules' kotlin-jvm 1.9.24). Verified: every native lib in the APK (libsodium/libjnidispatch + the litert/tflite set) is `p_align` 0x4000. Identity at rest: `AndroidKeystoreKeyStore` (androidMain) seals the X25519 private key with **Keystore-direct AES-256-GCM** (`KeystoreAesKey` hardware-backed AES key + `AesGcmEnvelope`, written to `relay_identity.x25519.gcm`) — the hardware Keystore can't hold a raw X25519 key, so it's encrypted-at-rest, not hardware-resident. **Security L4 (PR #19) removed the deprecated alpha androidx `EncryptedFile`/`EncryptedSharedPreferences`** here and in `SecureStorage.android.kt`, replacing both with this Keystore-direct envelope (one AES key, plaintext key names, encrypted values); the androidx dep is gone. Clean break, no read-old migration — `CleanBreakReset` (top of `AndroidDatabaseFactory.create`, before the M1 loss-guard) wipes a pre-L4 install's orphaned encrypted DB + legacy files so an in-place upgrade re-pairs instead of bricking. **#40 holds:** the APK packages `libsodium.so` + `libjnidispatch.so` alongside `libLiteRt.so`/`libLiteRtClGlAccelerator.so`/`liblitertlm_jni.so` — distinct names, no collision, litert packaging unchanged; verified on a Pixel 7 (classifier+embedder+Gemma load; `RelayCryptoSmokeTest` passes). The full live-relay run (Stripe subscribe → relay QR → camera scan → chat+sync → `4004` fallback) stays a manual procedure; the chat-header relay status dot is still deferred (cosmetic). **Relay UX + reconnect (local-agent PR #77 / secure-gateway PR #9, validated on Pixel 7):** unpair (mobile) / disconnect (desktop) revoke through the gateway via the `RelayDisconnector` seam; transport-aware status labels (gateway vs LAN, both sides). **Reconnect is "pair once, reconnect many" — the QR pairing token is single-use, so replaying `pair()` on reconnect 401s.** Mobile persists `{pairingToken,deviceId,pairId,desktopPublicKey}` (`SecureStorage` `RELAY_PAIRING_STATE`) and, when the saved token matches the scanned QR, restores it via `MobileConfig.pairId`/`desktopPublicKeyB64` + `MobileClient.isPaired()` → `connect()` only (else fresh pair). Desktop persists its **device id** (`RELAY_DESKTOP_DEVICE_ID`) and restores it into `DesktopConfig.deviceId` (read back via `DesktopClient.deviceId()`) so a restart re-mints as a **re-pair** (gateway reuses the `max_pairs` slot + same `pair_id`) instead of registering a new `dev_…` and failing `capacity_exceeded` → silently falling back to the LAN QR. (Establishing the desktop id needs ONE slot-free mint — Unpair the phone or reset prefs once.) SDK fixes: `AuthClient` on `java.net.HttpURLConnection` (java.net.http absent on Android), `unpair()` both clients, crash-safe `ConnectionManager.close()`; gateway billing reuses the customer's account when `subscription.created` precedes `checkout.session.completed` (was `404 license_not_found`). **Local test needs a shared Redis backplane** (`AUTH_BACKPLANE=redis`/`RELAY_BACKPLANE=redis`) or revocation never reaches the relay (desktop sees only `PEER_OFFLINE`, never `REVOKED`); QR endpoints must be the LAN IP, not `127.0.0.1`. `RELAY_ONLY_DEBUG` relay-pin **removed** (LAN fallback restored); the `[sdk]`/`[Relay/scan]` diagnostic logging is **kept** for ongoing UAT on main (strip once signed off). See `docs/ANYWHERE_ACCESS_PLAN.md`.

56. **LAN link REMOVED — the Secure Gateway relay is the SOLE mobile↔desktop pairing path (PR #80).** The original LAN link (#57: `magent://` QR, plain-HTTP Ktor link server, LAN IP discovery) caused runtime networking conflicts + poor UX, so it's gone on both platforms. **Net effect:** (a) the desktop pairing QR renders **only while a subscription is active** (`DesktopLinkPairingControls.desktop.kt` gates on `subscription.isActive`; `Main.kt` publishes `relayQrPayload` directly — no LAN-QR `combine`, so it's null unless subscribed); (b) the QR carries **only** the relay payload (`RelayQrPayload`); (c) the mobile **Jobs icon** shows only when paired (`ChatScreen` gates on `isDesktopPlatform || DesktopLinkStatus != DISABLED`); (d) **remote LLM through the relay works unchanged** — a relayed CHAT hits `DesktopLinkRequestHandler` → `warmModel.session()` → the desktop's `RoutingInferenceEngine` singleton, so a remote Ollama/OpenAI configured on the desktop serves the turn instead of local llama.cpp (locked by `DesktopLinkRelayRoutingTest`). **Deleted:** `LanLinkTransport`, `LinkPairingPayload`, `LinkAccessMode`, `LanAddress`, `DesktopLinkClient`. **Slimmed:** `DesktopLinkConfig` drops `peerHost/peerPort/pairingToken/accessMode/baseUrl()` (prefs decode with `ignoreUnknownKeys`, so old blobs still load); `DesktopLinkServer` keeps only the loopback `/ping` + `/subscribe/callback` (the Stripe redirect — still load-bearing for #54); `MobileLinkKind` loses `LAN`; `DefaultLinkTransportProvider`/`PollingDesktopLinkStatusProvider` are relay-only (status mirrors the relay pipe, no `/health` poll). The `LinkTransport` seam + `DesktopLinkRequestHandler` (now driven only by the relay `FrameDispatcher`) stay. The desktop-link `OllamaConnectionMonitor` health probe now runs a `HEALTH` unary over the current relay transport. **Two relay-host fixes rode in with #80** (both `docs/ANYWHERE_ACCESS_PLAN.md`): (i) **`bad_devices` self-heal** — a persisted `RELAY_DESKTOP_DEVICE_ID` the gateway no longer knows (re-claim under a new account, or `AUTH_STORE=memory` reset) made `createPairingToken` 400; the LAN QR used to mask it, so now `DesktopRelayHost.generatePairingQr()` clears the stale id + re-registers + retries once (only `bad_devices`, NOT `capacity_exceeded`). (ii) **Relay X25519 identity moved off-disk** — the SDK's default `FileKeyStore` wrote the private key to a **plaintext** `relay_identity.key` (hex, `chmod 0600`); `SecureStorageKeyStore` (desktopMain, implements the SDK `com.securegateway.core.keystore.KeyStore`) now keeps it hex-encoded in the encrypted `secrets.p12` under `RELAY_IDENTITY_KEY`, migrating the legacy file once then deleting it — wired via `DesktopConfig.keyStore` (not `keyStoreFile`). Tier = the `DesktopSecureStorage` PKCS#12 tier (per-user-derived password or `LOCALAGENT_KEYSTORE_PASSWORD`), matching the account secret; full OS-keyring is still the deferred hardening. **This supersedes the LAN-specific bits of #54/#55/#57 and obsoletes draft PR #79 (LAN-disconnect fix).** **Desktop reconnect-without-re-pairing (PR #91):** since the LAN QR is gone, a desktop restart used to re-mint a fresh single-use pairing token (rotating the QR → phone must re-scan, and Unpair-to-free-the-slot first). Now symmetric with the mobile half (#55): `DesktopRelayHost` persists `{pairId, mobilePublicKey}` (`RELAY_DESKTOP_PAIR_ID`/`RELAY_MOBILE_PUBLIC_KEY` in `secrets.p12`) after pairing and on each launch tries `reconnect()` (seed `DesktopConfig.pairId`/`mobilePublicKeyB64` → `DesktopClient.isPaired()` → `connect()` only, **no QR/no re-scan**) **before** `Main.kt` mints a QR; falls back to mint+`awaitPairing` only when there's no saved pairing or the reconnect hits a **dead** pair (`connect()`'s sync `issueToken` throws `AuthException` with a non-zero `httpStatus()`; a `transport_error`/httpStatus 0 is transient → keep the saved pairing). Desktop Disconnect + peer REVOKED clear the saved pairing (keep `RELAY_DESKTOP_DEVICE_ID` so the next fresh QR reuses the slot). The gateway needed no change (token issuance already validates an active pair); the secure-gateway side is SDK-only (`DesktopConfig.pairId`/`mobilePublicKeyB64`, `DesktopClient.isPaired()`/`mobilePublicKeyB64()`). See `docs/ANYWHERE_ACCESS_PLAN.md`. **Desktop "Pair Now" on-demand QR (PR #92):** the relay pairing token is only valid ~300s, so the desktop no longer **auto-mints** the QR on subscribe / clean startup / Disconnect (it was usually stale before anyone scanned). A **"Pair Now"** button mints on click, shows the QR for one 300s window with a **live countdown**, then auto-hides so the button returns. New **`RelayPairingInitiator`** seam (commonMain, mirrors `RelayDisconnector`; desktop=`DesktopRelayHost`, mobile/iOS=`NoOpRelayPairingInitiator`); `DesktopRelayHost` exposes `pairRequests: SharedFlow<Unit>` + `requestPairing()` + a `companion PAIRING_WINDOW`. `Main.kt`'s no-reconnect branch now loops `pairRequests.first()` → mint + publish `relayQrExpiresAt` → `awaitPairing(PAIRING_WINDOW)` → `connectAndServe` (break) or `close()`+loop-to-button on timeout; **the reconnect path is unchanged** so a paired phone still reconnects silently (button only shows for `UNPAIRED`). `DesktopLinkQrProvider` gains `qrExpiresAtEpochMs` (`set(payload, expiresAt)`); `SettingsViewModel.requestPairing()` + `SettingsUiState.desktopLinkQrExpiresAtEpochMs`; the desktop `DesktopLinkPairingControls` renders the button + a 1s-ticker countdown off the published deadline. No DB migration / gateway / SDK change.

### Relay reconnect server-side cleanup (PR #30)

77. **`FrameDispatcher` MUST drain its in-flight registry (cancel every `streamJobs` entry + reset `unaryInFlight` to 0) whenever `pipe.state` ≠ `UP`, symmetric with the client `FrameMultiplexer.failAllInFlight()`.** The relay pipe survives a peer backgrounding (`PEER_OFFLINE → DOWN`) and returning (`CONNECTED → UP`) — **same pipe, same `FrameDispatcher`, same `streamJobs` map**. The server's slots were only freed by an *active* send failing (peer-offline) or an explicit `CANCEL`, but the idle `SYNC_SUBSCRIBE` stream (`DesktopLinkRequestHandler.subscribeStream()` blocks in `localChanges.collect` and never `send`s without a change) never trips either, so it **leaks one `maxConcurrentStreams` (=8) slot per background/foreground cycle** — and the mobile's background-time `CANCEL` is swallowed (pipe already DOWN), so the desktop never hears it. After ~6–8 cycles the cap is exhausted and **every** new stream — including `CHAT` — is rejected `429` (`STATUS_OVERLOADED`), which the phone surfaces as *"Sorry, I had trouble processing that request."* The symptom is **purely the concurrency cap, NOT a key-rotation/crypto desync** (a re-key desync surfaces as status-0 "relay unreachable", not 429 — don't chase the security PRs for this). Fix: a `pipe.state` collector in `FrameDispatcher.start()` calls `drainInFlight()` (snapshot+clear `streamJobs` and zero `unaryInFlight` under the mutex, then `cancel()` the jobs outside the lock); the unary `finally` decrement is `coerceAtLeast(0)` so a concurrent reset + late decrement can't underflow the counter and over-grant the cap. The caps themselves (security M5 / audit F4) did not cause the leak — they made a pre-existing teardown gap fatal. Covered by `FrameDispatcherLimitsTest` (drain-on-DOWN, ×10-cycle CHAT-still-accepted, unary no-underflow).

### Rename + build/launch robustness (PR #94)

PR #94 renamed the Kotlin/Java namespace + `applicationId` + desktop data-dir + `LOCALAGENT_*` env vars `mobileagent`→`localagent` (a full rebrand; a **fresh install on both platforms** — no data/pairing/subscription/aux-model carries over) and added three robustness guards.

61. **ALL models auto-download from the models CDN on first run on BOTH platforms (aux PR #3; Gemma + Vosk moved off HuggingFace in PR #22; host moved off R2 to `downloads.contextsolutions.com/models` in PR #28); never bundled, NO auth.** Hosting is `https://downloads.contextsolutions.com/models` (the `downloads` host serves media by path; model files live flat under `/models/<filename>`), sha256+size-verified by the resumable downloaders. PR #22 deleted the `HfAuthTokenProvider`/`requiresHfAuth`/`HF_AUTH_TOKEN` plumbing — no Authorization header is ever sent. Filenames: Android `gemma-4-E2B-it.litertlm` + `preflight_memory_shared_v1.0.0_int8.tflite` + `all-MiniLM-L6-v2_int8.tflite`; desktop `gemma-4-E2B-it-Q4_K_M.gguf` + `gemma-4-E2B-it-mmproj-F16.gguf` + `preflight_memory_shared_v1.0.0.onnx` + `all-MiniLM-L6-v2.onnx` + `vosk.tar.gz` (see #48).
    - **Desktop** (`.onnx`): `DesktopAuxModelStore` (`inference/`) ensures `preflight_memory_shared_v1.0.0.onnx` + `all-MiniLM-L6-v2.onnx` into `<app-data>/models/` via `DesktopModelDownloader` (sha256 + byte size **pinned in `DesktopAuxModels`**), then `Main.kt` warms the engines. Precedence per model: `LOCALAGENT_{CLASSIFIER,EMBEDDER}_ONNX` env override → already-present (size match) → download → skip. **The base URL defaults to the CDN** (`DesktopAuxModels.DEFAULT_BASE_URL`) so a normal build auto-downloads; **`-PauxModelBaseUrl=https://host/path`** (baked into `desktop_build_info.properties` by `:desktopApp`, read by `DesktopAuxModels.baseUrl()`) overrides the host. (PR #3 fixed the prior bug where the default was an unconfigured placeholder ⇒ `isConfigured` false ⇒ the store skipped the fetch ⇒ pre-flight `FallThrough(ClassifierUnavailable)` ⇒ search never fired.) `DesktopAuxModelStoreTest` covers the decision paths.
    - **Android** (`.tflite` + `.litertlm`): `AndroidAuxModels` defines the two aux `ModelSpec`s and `ModelInventory.SPEC` the Gemma one — all three pin the R2 URL + sha256/size **directly** (PR #22 — the Gemma coords are no longer read from `secrets.properties`/`BuildConfig.MODEL_*`; `ModelSpec.requiresHfAuth` is gone). `ModelInventory.requiredSpecs()` = Gemma + both aux; the first-run `DownloadScreen` gate fetches them all via `ModelDownloadWorker` (one job, aggregate progress) and chat unlocks only when `allRequiredPresent()`. `ModelDownloader.download(spec, …)` is spec-parameterized and sends **no** auth header. The engines load from `filesDir/models/` **only** (bundled-asset fallback removed); absent ⇒ `warmUp()` null ⇒ degrade to Gemma-only.

62. **Android crash-reporter + analytics gate on Firebase being initialized — a missing `google-services.json` degrades to no-op, never crashes launch.** `google-services.json` is gitignored (absent in CI / fresh checkouts, and after the rename its `package_name` no longer matched the new `applicationId`), so the google-services plugin isn't applied and `FirebaseApp` never auto-inits. `AndroidKoinModule` binds `FirebaseSafeCrashReporter` only when `FirebaseApp.getApps(ctx).isNotEmpty()`, else `NoOpSafeCrashReporter`; `LocalAgentApplication.onCreate` likewise guards `FirebaseAnalytics.getInstance(this)` + the consent binding. Without the guard both threw `Default FirebaseApp is not initialized` on launch. Firebase telemetry still works unchanged when a matching json is present (add the new package in the Firebase console + drop in `androidApp/google-services.json`).

63. **A debug DEVICE build (`assembleDebug`/`installDebug`) fails fast when `android-app/secrets.properties` is absent.** It now bundles only the **dev Brave key** (`BRAVE_DEV_KEY`); PR #22 removed the HF token + Gemma `MODEL_*` checksum entries (models download from the public models CDN `downloads.contextsolutions.com/models` with no secrets). `androidApp/build.gradle.kts` throws at **configuration time** — gated on the requested task names, so it fails before the compile and **only** for those two tasks. Release builds (empty keys by design / BYOK), unit tests, the desktop build, and CI (which never assembles the APK) are unaffected.

### English-only launch + "Todos" → "My List" rename (PR #99)

64. **Launch is English-only: the language picker is gated on `PreferredLanguage.selectable` (= `listOf(EN)`), NOT `entries`.** Both dropdowns (onboarding `LanguagePickerScreen`, Settings `SettingsScreen`) iterate `selectable`; the full 10-language `entries`, the `StringCatalog`, `LanguagePreferences`, and the Spanish pack (`strings_es.json`, kept in i18n parity) all stay wired — re-enabling a language is a one-line edit to `selectable`. A device already persisted to ES still resolves; only the picker is constrained.

65. **The personal-checklist feature is "My List" (code prefix `MyList`/`mylist`), NOT "Todo" — STT mis-hears "todo" but transcribes "my list" reliably.** Full rename of PR #15's surface: SQLite table `todos` → `mylist` (migration `10.sqm`, schema **v11**; pure `ALTER TABLE … RENAME` + index swap, rows migrate in place — generated types are now `MyListQueries`/`Mylist`/`db.myListQueries`); package `…todo` → `…mylist`; `Todo`→`MyListItem`, `TodoPriority`→`MyListItemPriority`, repos/VM/screen/`MyListCommand` (its `.List` subtype → `.Show`)/`MyListRef`/parser/detector/formatter/tool-handler all `MyList…`; `MainRoute.MyListManagement` (route `"mylist"`); `StringKeys` `MYLIST_*`; tool-name constants `add_mylist_item`/`show_mylist`/`complete_mylist_item`/`delete_mylist_item`/`edit_mylist_item`/`clear_completed_mylist_items`. **The `tasks` table/feature (`Tasks.sq`) is unrelated — do NOT fold it in.** `// TODO:` code comments are left untouched.

66. **My List intent matching REQUIRES the literal phrase "my list" — bare "list"/"todo"/"task"/"checklist" deliberately do NOT fire.** `MyListIntentDetector` gates on `\bmy\s+lists?\b`; `MyListCommandParser` returns null without it, then peels off the `<preposition> my list` locator ("on/to/from my list") before running the verb grammar, so "complete #2 **on my list**" resolves like the old bare "complete #2" while bare "complete #2" (no "my list") is now ignored. The `includeCompleted` view keeps "my list" adjacent ("show **all of** my list" / "show my list **including completed**"), never inserting a word between "my" and "list". Rationale: "list"/"todo" are far too common to gate on, and the surface is named "my list" everywhere the model is instructed.

### Desktop Choose Job catalog + bundled library + pre-save init (PR #100)

67. **Desktop **Choose Job** is a curated catalog from the bundled `agent-jobs` library (NOT the old Swing file picker, which is DELETED), and a chosen job runs its declared `init` before it can be saved.** Three coupled parts, desktop-only:
    - **Bundle + copy:** `desktopApp/build.gradle.kts`'s `bundleAgentJobs` `Zip` task zips the repo-root `agent-jobs` submodule into the classpath resource `/agent-jobs.zip` (excludes `.git`/`.claude`/`node_modules`/`.env`/`seen.json`/`.localagent-init.json`; `isReproducibleFileOrder=true`+`isPreserveFileTimestamps=false` for a deterministic archive), mirroring the `generateDesktopBuildInfo`→`resources.srcDir`+`processResources.dependsOn` pattern. `DesktopJobLibraryStore.ensure()` (`Main.kt` startup, best-effort `runCatching`) **overlay**-extracts it to `<app-data>/agent-jobs` gated by a `.deployment` stamp (`"${versionCode}-${gitDescribe}"` from `AppBuildConfig`): same stamp → no-op; else overwrite bundled files but NEVER delete the dir (user files — `node_modules`/`.env`/`seen.json`/the init marker — survive a new deployment). Zip-Slip guarded. **Overlay never prunes a job folder removed upstream** (accepted).
    - **Catalog:** `DesktopJobCatalog` reads `<library>/job.list.json` (`@Serializable JobList(version, jobs:[{name,path}])`) and joins each entry with its `job.settings.json` via the existing `JobSettingsLoader`, building `JobCatalogEntry(id, displayName, description, programPath, workingDir, supportedOnThisOs, requiresInit)`. Program resolved for the host OS (`resolveProgram`/`currentOsKey`); no entry ⇒ `supportedOnThisOs=false` (row shown disabled). Missing/unreadable manifest ⇒ skipped.
    - **Pre-save init:** `JobSettings` gains an optional `init: JobInitSpec` (`requires:[String]` + `steps:[JobInitStep]` + OS-keyed `verify`). `DesktopJobInitializer` builds ONE ordered `InitUnit` list shared by `plan()` (the checklist the UI shows up front) and `initialize()` (execution): `requires:["node"]` → a runtime unit (see runtime bullet); a step is non-interactive (`run` OS-map, must exit 0) **or** interactive (`launch` OS-map + `interactive`: shows `instructions`, launches the command — e.g. a real Chrome window to clear realtor.ca's Incapsula check — waits for the user to close it; only a launch that never STARTS fails — exit code ignored); then OS-keyed `verify` (must exit 0). Steps with no command for the host OS are omitted. Commands are full shell strings (`sh -c` / `powershell -Command`) run in the job dir — **manifest = trusted bundled content, so NO injection-safe positional binding** (unlike `JobExecutor`'s user-keyword arg, #49). `initialize` emits `JobInitProgress(index, state, message?)` per unit (`PENDING/RUNNING/AWAITING_USER/DONE/FAILED`); **on success** writes a `.localagent-init.json` marker (manifest `version`) and returns `Succeeded`/`AlreadyInitialized` (a later choose, or a new deployment that leaves the marker, short-circuits); **on failure** returns `Failed(stepTitle, reason)`. Waits are cancellation-aware (`currentCoroutineContext().ensureActive()` poll loop + `finally` `destroyForcibly`, mirrors #29/#59).
    - **Executable bit:** `java.util.zip` (`ZipInputStream`) DROPS the unix `+x` bit, so a freshly extracted `watch.sh` fails "permission denied" on Linux/macOS. `DesktopJobLibraryStore.markExecutables` re-`setExecutable`s each `job.settings.json`'s `program[os]` files + any `*.sh` after extracting (no-op on Windows). Re-runs on each overlay extract (the file is overwritten, losing +x again).
    - **Local Node:** a node job opts in via `init.requires:["node"]`. `DesktopNodeProvisioner.ensure` returns the **system** node when `node --version` + `npm --version` both succeed; else downloads the pinned Node LTS (`NODE_VERSION`, dist mirror overridable via `LOCALAGENT_NODE_DIST_BASE`) for the host OS/arch and extracts it (system `tar`) into the stable `<app-data>/runtimes/node`. `DesktopJobRuntimeEnv.applyTo` prepends that private bin dir to the subprocess `PATH` for BOTH init steps (`npm install`) AND `JobExecutor` job runs — without it a scheduled run wouldn't find `node`. No-checksum-yet (verified by running `node --version`); sha-pin is a follow-up.
    - **Seam + UI:** `JobCatalog`/`JobInitializer` are commonMain seams in `shared/commonMain/job`, bound only on the desktop (`DesktopModule`); `JobsViewModel` resolves them via `getOrNull()` → `canChooseJob` false on mobile so the button is hidden. `JobsScreen`'s `ChooseJobDialog` (commonMain) is **two-phase**: (1) pick from the catalog list; (2) show the job description + `plan()` checklist with live per-step status, an **always-present Cancel** that aborts (cancels the coroutine → kills the subprocess), and an **Approve** button (only once every step succeeds) that fills the form's `command`+hidden `workingDir` and returns to the New Job page for name+schedule. On failure the failing step + reason show and `command` stays blank → **Save stays disabled** (the form can't produce a non-initialized save — that IS the block). The `init` schema/manifest edits land in the separate **agent-jobs** repo; this PR also bumps the submodule pointer. See JOBS_PLAN §0.

### Chat message delete / sync (PR #4)

68. **Deleting a chat response is a per-TURN soft-delete (tombstone), never a per-bubble hard delete — so it propagates over relay sync instead of being resurrected.** The assistant-bubble "x" deletes the whole conversational turn (the user message + its assistant answer(s) + any interleaved tool turns), behind an `AlertDialog` confirm. `messages.deleted_at_epoch_ms` (migration `11.sqm`, schema **v12**) mirrors the conversations soft-delete (`8.sqm`/`deleted_at`). **Two read paths, deliberately split** (like conversations): `selectActiveMessagesByConversation` (tombstones excluded) backs `loadMessages`/`loadMessagesWithIds` + `deleteOldestPair` + the UI/agent history; the **raw** `selectMessagesByConversation` KEEPS tombstoned rows so (a) the sync bundle still carries the delete and (b) `appendMessage`'s next-`sequence_index` never reuses a tombstoned row's index (`selectLastMessagePreview` also filters tombstones). `ConversationRepository.deleteTurnContaining(convId, messageId)` finds the turn boundaries by user-message runs, `softDeleteMessagesInRange`s the `[startSeq,endSeq)` range, **bumps `conversation.updated_at`** (so the tombstones ride `selectConversationsChangedSince`), and returns the deleted ids — `ChatViewModel.deleteTurn` prunes those bubbles in place and reloads `agentHistory`. **Sync is tombstone-wins / no-resurrection:** `upsertMessageFromPeer` is no longer `INSERT OR IGNORE` — it's `INSERT … ON CONFLICT(id) DO UPDATE SET deleted_at_epoch_ms = excluded.deleted_at_epoch_ms WHERE excluded.deleted_at_epoch_ms IS NOT NULL AND messages.deleted_at_epoch_ms IS NULL` (content columns stay immutable; a peer's NULL can never clear a tombstone). `MessageSyncRecord.deletedAtEpochMs` carries it both directions. **Trim-from-here (delete this turn + everything after) is deferred** — building block exists (`deleteMessagesBelowSequence`). New-migration dance per #20; schema-version pin in `MemoriesMigrationTest` is **13** (PR #22's `12.sqm` added `mylist.deleted_at_epoch_ms`, see #75).

### Release build — R8/minify + signing (PR #7)

70. **The minified release build (`isMinifyEnabled = true`, `proguard-android-optimize.txt`) needs five hand-added R8 keep/dontwarn rules, and `assembleRelease` succeeding does NOT mean the APK runs — R8 can't see reflective, JNI, Jackson, or JNA-by-name lookups, so always install + launch a release build AND exercise each feature (chat, pairing) to verify (debug never exercises R8).** `androidApp/proguard-rules.pro` carries (a) `-dontwarn org.tensorflow.lite.Delegate` — `play-services-tflite-gpu`'s `GpuDelegate` references `org.tensorflow.lite.Delegate`, which lives in the `tensorflow-lite-api` artifact excluded everywhere per #18, so R8 fails the **build** on the dangling reference; the path is dead at runtime (classifier/embedder = litert CPU XNNPACK #18, LiteRT-LM GPU = play-services' own delegate #3), so suppressing is correct, not a cover-up; (b) `-keep class * extends androidx.room.RoomDatabase { <init>(); }` — WorkManager builds Room's generated `WorkDatabase_Impl` reflectively via its no-arg ctor (`androidx.startup.InitializationProvider` → `WorkManagerInitializer` → `getDeclaredConstructor().newInstance()`), and the optimizer strips that "unused" ctor → the build is green but the app **crashes at launch** with `NoSuchMethodException: androidx.work.impl.WorkDatabase_Impl.<init> []`; and (c) `-keep class com.google.ai.edge.litert.** { *; }` + `-keep class com.google.ai.edge.litertlm.** { *; }` — both native runtimes reach back into Java via **JNI `FindClass`/`GetMethodID` by fully-qualified name** (R8 can't see those string refs), so R8 shrank `LiteRtException` and `CompiledModel_nativeCreateFromFile` **SIGABRT**ed during classifier warm-up (`litert_jni_common.h: Check failed: ex_class != nullptr / Failed to find LiteRtException class`) — a native abort, not a Java `FATAL EXCEPTION`, surfacing only in the tombstone/`DEBUG` logcat; keeping both packages' names + members makes every JNI lookup resolve (and lets the normal #18 GPU-refusal exception be thrown + caught → CPU fallback, instead of aborting). and (d) `-keep class com.securegateway.** { *; }` + `-keepattributes Signature` — the relay SDK's model types (`com.securegateway.core.auth.QrPayload`, link frames, auth responses) are deserialized by **Jackson** via reflection; R8 obfuscated them, so relay **pairing** failed at `decode qr payload` (`Cannot construct instance of <obfuscated> (no Creators … exist): abstract types either need to be mapped to concrete types …`), which the UI surfaced as **"gateway unreachable"** even though the gateway is never contacted (the QR parses — `[Relay/scan] matched RELAY` — then `AndroidRelayBytePipeFactory.create` throws). `Signature` keeps Jackson's generics (the QR's `Map<String,String>` endpoints); and (e) `-keep class com.sun.jna.** { *; }` (+ `-keep class * extends com.sun.jna.**`, `-keepclassmembers class * extends com.sun.jna.**`, `-keep class com.goterl.** { *; }`, `-dontwarn java.awt.**`) — the SDK's `Crypto` signs/encrypts via **lazysodium → native libsodium → JNA**, and JNA's native dispatcher (`libjnidispatch.so`) reads `com.sun.jna.Pointer`'s `peer` field (+ other `Structure`/`Library` members) back through JNI **by name**; R8 renamed them, so pairing failed (one step *past* the QR decode) with `UnsatisfiedLinkError: Can't obtain peer field ID for class com.sun.jna.Pointer` → `NoClassDefFoundError: com.securegateway.core.Crypto` (static-init failure) at `MobileClient.<init>` / `AndroidKeystoreKeyStore.loadOrCreateIdentity`. These are JNA's documented ProGuard rules plus a lazysodium keep. The existing `-keep class com.contextsolutions.localagent.** { *; }` + `-keepattributes *Annotation*` already cover our kotlinx-serialization (and the SDK's Kotlin `@Metadata`/Jackson annotations); Koin/Ktor/Compose needed nothing. **Don't "fix" R8 breakage by disabling minify/shrinkResources** — both stay on. **Diagnostic tags:** relay pairing logs under logcat tags `Relay` (byte pipe + `[sdk]` SDK lines) and `DesktopLink`, plus `System.out` for the `[Relay/scan]` QR-parse `println`s — `adb logcat -s Relay:I DesktopLink:I System.out:I AndroidRuntime:E`. **Release signing:** a release `signingConfig` is wired from `secrets.properties` (`RELEASE_STORE_FILE` + `RELEASE_STORE_PASSWORD`/`RELEASE_KEY_ALIAS`/`RELEASE_KEY_PASSWORD`); absent → falls back to `~/.android/debug.keystore` so `./gradlew :androidApp:installRelease` works out of the box for on-device testing; no keystore at all → release stays **unsigned** (CI never assembles the APK). **A debug-signed release is for local verification ONLY** — Play needs a real upload key + App Signing. Release `applicationId` has **no `.debug` suffix** so it installs alongside a debug build.

### PR #22 — feature batch (merged)

71. **Timers ring continuously like alarms (Android), with Stop + "+1 minute" instead of snooze.** Timers used to fire a single silent-on-DND chime; now `ClockEventReceiver.onTimerFire` starts a foreground **`TimerFiringService`** (modelled on `AlarmFiringService`: looping `USAGE_ALARM` `MediaPlayer` + vibration + 10 s notification re-post on a dedicated `clock_timer_firing` channel) that rings until the user acts. The ringer notification's two actions route through `ClockActionReceiver`: **Stop** (`ACTION_STOP_TIMER` → silence) and **+1 minute** (`ACTION_ADD_MINUTE` → silence + `ClockService.createTimer(60_000, label)`, the timer analogue of snooze but additive). `TimerFiringService` declared in the manifest with `foregroundServiceType="mediaPlayback"` like the alarm service.

72. **The mobile chat model-load banner shows a small fixed state set; desktop keeps its accurate accelerator banner.** `SessionBanner` (`ui/.../chat/ChatScreen.kt`) branches on `isDesktopPlatform`: mobile maps `SessionState` to `CHAT_SESSION_MOBILE_*` — *Model downloading … N%* / *Model loading* / *Model loaded on GPU* / *Model loaded on CPU* / *Model unloaded* (Failed→unloaded; Remote→hidden). Desktop still uses the per-accelerator `CHAT_SESSION_*` strings (it runs CPU/Vulkan/remote where "GPU" would lie).

73. **Remote LLM connection AND Web Search both default OFF (opt-in).** `OllamaConfig.enabled` defaults **`false`** (was `true`); the gate stays `isActive = enabled && isConfigured` (#44), so a configured server is inert until the user flips the switch on. `SecureStorageKeys.SEARCH_ENABLED` unset now means **disabled** — the Settings VM reads `== "true"` and the 4+4 `SearchService` `isEnabled` gates in `AndroidKoinModule`/`DesktopModule` read `== "true"` (was `!= "false"`). The Settings web-search section keeps the "add a Brave key" warning.

74. **HuggingFace is fully removed; onboarding is Language → Disclosure → Complete.** Deleted: `HfAuthTokenProvider`/`DefaultHfAuthTokenProvider`, `ModelSpec.requiresHfAuth` + the `ModelDownloader` bearer branch, `SecureStorageKeys.HF_AUTH_TOKEN`, `AppBuildConfig.hasHfDevToken`, and the `MODEL_DOWNLOAD_URL`/`MODEL_SHA256`/`MODEL_SIZE_BYTES`/`HF_AUTH_TOKEN` BuildConfig + `secrets.properties` entries (models now pin R2 coords directly, #61). The **HuggingFace Settings section** and the **HF, Brave, telemetry, AND country/location onboarding screens/steps** are gone (PR #22 dropped HF/Brave/telemetry; **PR #23 dropped the country/location step** — `OnboardingStep` = Language/Disclosure/Complete; `OnboardingPreferences` is down to the language + disclosure gates; `MainViewModel.onboardingComplete` = `languageDecided && disclosureAcknowledged`). **Telemetry already defaults OFF** (`TelemetryConsentManager.DEFAULT_ENABLED = false`) — removing its onboarding page changes nothing but where you opt in (Settings). Brave search stays BYOK via Settings. **Search-defaults country defaults to USA** (the repos fall back to `US` when no location is saved — `DataStore`/`Desktop`SearchPreferencesRepository `DEFAULT_COUNTRY`), changeable in Settings → Search sources (#76); the `SearchSourcesViewModel.SELECTABLE_COUNTRY_CODES` order (`US`, `CA`) makes USA the picker's default selection.

75. **My List runs on desktop too and syncs bidirectionally mobile↔desktop.** The chat-header My List icon is no longer `!isDesktopPlatform`-gated (timers/alarms stay mobile-only). Sync follows the memories/messages tombstone pattern (#51/#68): `12.sqm` adds `mylist.deleted_at_epoch_ms` (schema **v13**); `MyList.sq` gains `selectMyListChangedSince` + `upsertMyListFromPeer` (LWW, tombstone) and soft-delete; `SqlDelightMyListRepository.flow()` is now a **reactive** `selectAll().asFlow().mapToList` (#51) + fires `LocalChangeBus`; `MyListSyncRecord` rides `SyncBundle`; `SqlDelightLinkSyncService` collects/applies it both directions. New-migration dance + schema pin per #20/#68.

76. **Settings → Search sources has a default-country picker, and chat "show #N on my list" renders a single item.** The Search-sources screen's top dropdown (auto-selects the current country; **restricted to CA + US** via `SearchSourcesViewModel.SELECTABLE_COUNTRY_CODES` — add codes there to expand) calls `SearchPreferencesRepository.resetToCountryDefaults(country)` which **overwrites** all five vertical lists with that country's defaults (no merge, unlike `setLocation`); the old "Defaults seeded from: …" line is gone. Chat: `MyListCommand.Show` gained an optional `ref`; `MyListCommandParser.parseShowOne` matches "show number 2 / #2 / item 3 / the milk item on/in my list" (digits AND spelled-out numbers via `wordToNumber`), and `MyListToolHandler.handleList` resolves the ref against the displayed list and emits one row (`single:true`). The list reply shows priority for every item but **notes only on a single-item show** (`renderRow(includeNotes = single)`). Also: the model **DownloadScreen** ("Set up the on-device models") is now fully on the i18n catalog (`StringKeys.DOWNLOAD_*`/`tr()`), copy reworded "Gemma 4" → "AI models".

### iOS / iPhone port (PR #41)

78. **iOS is a real target now: `:shared` + `:ui` compile + link an iOS `ComposeApp` framework, and on-device chat runs Gemma 4 E2B via the official LiteRT-LM Swift API (Metal), bridged into Koin — additive, Android/desktop untouched.** Architecture: the Swift app (`iosApp/`, hosts the LiteRT-LM SwiftPM package) implements the commonMain-exported **`NativeLlmBridge`** (callback-shaped: `load`/`generate(...)→NativeGenHandle`/`unload`; NO tool channel this milestone) and registers it via `IosEntryPointKt.doInitKoin(bridge:)`; **`LiteRtIosInferenceEngine`** (iosMain) adapts the bridge to the `InferenceEngine` seam (`suspendCancellableCoroutine` + `callbackFlow`, cancel→`NativeGenHandle.cancel`), and is the `local` engine inside `RoutingInferenceEngine` (remote Ollama still wins when active). **`IosChatSessionController`** keeps the model resident (no Android foreground service). **`iosModule`** (`fun iosModule(bridge): Module`, mirrors `androidModule`) + `agentCoreModule` + `uiModule` are started by `doInitKoin`; `MainViewController()` (`:ui` iosMain) hosts `AppNavHost` via `ComposeUIViewController`, gated on `IosModelInventory`/`IosModelDownloadController` (first-run Gemma `.litertlm` download, 2.58 GB, size-verified, `isExcludedFromBackup`). **On-device inference runs on a PHYSICAL iPhone only — NOT the Simulator** (CPU crashes, Metal absent; LiteRT-LM #2504); CI (`.github/workflows/ios-test.yml`, macos-latest) is compile + framework-link only. Platform seams: **`IosHttpEngineFactory`** (Ktor Darwin — `ktor-client-darwin` added to the catalog), **`IosSecureStorage`** (Keychain), **`IosDatabaseFactory`** (`NativeSqliteDriver`, **unkeyed** — SQLCipher-on-iOS deferred, `generateDatabaseKey()` still throws but is never called), preferences via **`IosJsonStore`** (NSDocumentDirectory JSON, one file per `Ios*Preferences`). **No-op this milestone:** classifier/embedder (`NoOp*Engine`, now in **commonMain** — moved from desktopMain), voice (`NoOpChatSpeaker`/`NoOpDictation`), telemetry (`NoOpAnalyticsSink`/`NoOpTelemetryFlusher` — `InMemoryTelemetryCounters` is JVM-only), relay/subscription/jobs-admin (`NoOpRelayDisconnector`/`NoOp*`, `PollingDesktopLinkStatusProvider(relayState=null)`→DISABLED). **Vision is now ON on iOS (PR #44) via the CPU/XNNPack vision backend — see #79** (was OFF in PR #41: routing the vision tower to Metal hit `STABLEHLO_COMPOSITE`). **`ComposeApp` is a DYNAMIC framework** (`isStatic=false`) + app/framework link `-lsqlite3`: the LiteRT-LM SwiftPM pkg forces `-all_load`, and a static framework would force-load Skiko's twice-bundled C libs → ~15.9k duplicate symbols. LiteRT-LM is pinned by **commit** (its target uses `unsafeFlags`, which SwiftPM forbids for versioned deps). Info.plist needs `CADisableMinimumFrameDurationOnPhone=true` (CMP `PlistSanityCheck`) + `ENABLE_PREVIEWS=NO` (preview dylib won't link the framework). **Clock timers/alarms fire via `UNUserNotificationCenter`** (`IosAlarmScheduler` — real, not no-op: schedules local notifications + a foreground-presentation delegate; recurring alarms re-arm only on next `rearmAll`). **Latent commonMain JVM-isms had to be fixed for Native** (they only compiled because Android+desktop are both JVM): `Dispatchers.IO`→**`expect/actual platformIoDispatcher`** (JVM=IO, Native=Default; ~20 callsites in `:shared`+`:ui`), `@Volatile`/`@Synchronized`→`kotlin.concurrent.Volatile`/removed, `StringBuilder.delete`→`deleteRange`, `sortedMapOf`/`Map.merge`/`String.format`/`System.currentTimeMillis`→common equivalents, `Throwable.stackTrace` copy→`expect/actual applyStackTraceFrom`. **Don't reintroduce these in commonMain** — the iOS compile gate catches them now. Relay is deferred because `com.contextsolutions.securegateway` ships no iOS artifact. **The Xcode project + the LiteRT-LM Swift symbol names in `LiteRtBridge.swift` are a scaffold** (can't be headless-verified) — finalize in Xcode (add the SwiftPM package, set the signing team). Full guide: `docs/IOS_BUILD.md`.

79. **iOS image input + vision is ON (PR #44), and the vision encoder MUST run on the CPU/XNNPack backend — requesting Metal for vision fails.** `LiteRtBridge.load()` sets `visionBackend = .cpu()` (NOT the text LLM's `.gpu`/Metal backend) when `enableVision`; the text prefill/decode graph stays on Metal, only the vision tower is CPU. Gemma 4 E2B's `.litertlm` pins `tf_lite_vision_encoder`/`tf_lite_vision_adapter` to `section_backend_constraint: cpu` and its 1477-op SigLIP encoder is fully XNNPack-delegatable on iOS (LiteRT-LM #2370); requesting the **Metal** vision backend instead routes to the compiled-model executor whose `STABLEHLO_COMPOSITE` op is **not registered in the iOS dylib** → `Node … (STABLEHLO_COMPOSITE) failed to prepare` → `Failed to create conversation` on every turn (why PR #41 shipped vision OFF; #2385 calls iOS vision a "known Metal constraint"). **E4B vision stays broken on iOS** (its encoder isn't fully XNNPack-delegatable — genuinely blocked upstream). Both `enableVision` DI sites flip true (`IosModule.kt` engine binding + `IosChatSessionController.InferenceConfig`; AND-gated at `LiteRtIosInferenceEngine`). Rest mirrors Android #39: image is a downscaled JPEG on the **trailing turn only**, attached in Swift via `Message(of: .imageData(Data), .text(text), role: .user)` (variadic `Content`, **NOT an array**; `KotlinByteArray`→`Data` per-byte copy in a Swift extension). **`IosImagePreprocessor`** (iosMain, behind the shared `ImagePreprocessor` seam) decodes via `UIImage(data:)` (bakes EXIF orientation), downscales longest edge to 768 rendering at `UIGraphicsImageRendererFormat.scale=1.0` (output px == points, not device-multiplied), JPEG@0.85; bound `single<ImagePreprocessor>` in `IosModule`. Picker = in-app **camera capture** via **`UIImagePickerController`** (`sourceType = .camera`, `ui/iosMain/…/chat/ImagePicker.ios.kt`, replaces the PR #41 no-op stub; mirrors Android's camera flow) — **requires the `NSCameraUsageDescription` Info.plist key** (iOS crashes accessing the camera without it; OS shows the camera-permission prompt on first use). Delegate (`UIImagePickerControllerDelegate` + `UINavigationControllerDelegate`) reads `UIImagePickerControllerOriginalImage` (a `UIImage`) → `UIImageJPEGRepresentation` → `preprocessor.toModelJpeg` off the main scope. No camera (Simulator) → `isSourceTypeAvailable` false → `onPicked(null)`. Display path (`decodeImageBitmap` via Skia, `messages.image_bytes` BLOB, staged chip + `UserBubble`) was already live cross-platform, no changes. **Benign runtime noise:** `libLiteRtTopKMetalSampler.dylib` isn't bundled → the top-K sampler falls back to the statically-linked CPU C API (`Metal sampler not available`) — correctness-neutral, affects only token sampling. LiteRT-LM stays pinned to the **v0.13.1 commit** (`a0afb5a5…`): no upstream vision fix exists (v0.14.0-alpha.0 doesn't register the op; #2370 open). Verified on a physical iPhone (receipt OCR). See `docs/IOS_BUILD.md`.

## Build & run

```bash
cd android-app
./gradlew :androidApp:assembleDebug
./gradlew :androidApp:installDebug
./gradlew :androidApp:installDebug -PuseStubEngine=true     # dev without real Gemma (StubInferenceEngine)
./gradlew :androidApp:installRelease                        # minified R8 release; signs via secrets.properties or debug-keystore fallback (#70)

# Launch / M0 spike harness
adb shell am start -n com.contextsolutions.localagent.debug/com.contextsolutions.localagent.app.MainActivity
adb shell am start -n com.contextsolutions.localagent.debug/com.contextsolutions.localagent.app.spike.SpikeActivity
```

**Skip the Gemma download during dev** (prod read path: `filesDir/models/gemma-4-E2B-it.litertlm`, written by `ModelDownloadWorker`):

```bash
adb push gemma-4-E2B-it.litertlm /data/local/tmp/
adb shell run-as com.contextsolutions.localagent.debug \
  sh -c 'mkdir -p files/models && cp /data/local/tmp/gemma-4-E2B-it.litertlm files/models/'
```

**Classifier + embedder are NOT bundled (PR #3).** The two aux `.tflite` download from the CDN (`AndroidAuxModels`) on first run into `filesDir/models/`, folded into the same first-run `DownloadScreen` gate as the Gemma LLM — chat unlocks only when `ModelInventory.allRequiredPresent()` (Gemma + classifier + embedder). The engines (`LiteRtClassifierEngine`/`LiteRtEmbedderEngine`) load from `filesDir/models/` **only** (no bundled-asset fallback); if absent, `warmUp()` returns null and the agent degrades to Gemma-only. To pre-stage the aux models during dev and skip the download, push them once (copy ONE file per `run-as cp` — multi-file `cp` mis-parses through the nested shell quoting):

```bash
PKG=com.contextsolutions.localagent.debug
for f in preflight_memory_shared_v1.0.0_int8.tflite all-MiniLM-L6-v2_int8.tflite; do
  adb push "models/$f" /data/local/tmp/
  adb shell run-as $PKG cp /data/local/tmp/$f /data/data/$PKG/files/models/$f
  adb shell rm -f /data/local/tmp/$f
done
```

On a successful load: `loading classifier from filesDir (…)` then `classifier loaded on CPU` (the `GPU init failed; falling back to CPU XNNPACK` line is normal — #18's GPU refusal).

**Wireless adb** (Pixel 7's USB is unstable — pair once, re-`connect` after each reboot; port changes, pairing persists):

```bash
adb pair <phone-ip>:<pairing-port>     # one-time, 6-digit code
adb connect <phone-ip>:<connect-port>  # after reboot or WiFi drop
```

**Classifier training pipeline (M3/M4/WS-14):**

```bash
cd classifier-training
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"        # gen, review, dedup, stats CLIs
pip install -e ".[training]"   # adds torch/transformers/litert-torch (CUDA)
```

Reproduce v1.0 (~10 min on RTX 5090): `ct-train-classifier` → `ct-eval-classifier` → `ct-export-litert`. Gen is Ollama-backed (`qwen3.5:9b`); `CT_GEN_BACKEND=claude` + `ANTHROPIC_API_KEY` for the Claude path. Full CLI list in `classifier-training/pyproject.toml`.

WS-14 regression gate (before any new `.tflite` lands in `models/`) — exit 0 PASS / 1 SHA-mismatch / 2 regression / 3 infra:

```bash
ct-regression-check --ckpt ../eval/runs/<ts>/best.pt
ct-regression-check --skip-eval --ckpt path/to/metrics.json   # hosted-CI flow
```

**Instrumentation tests (Pixel 7):**

```bash
./gradlew :androidApp:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=com.contextsolutions.localagent.classifier.ClassifierLatencyBenchmark
```

Other classes: `ClassifierEndToEndTest`, `EmbedderEndToEndTest`, `MemoryRetrievalLatencyBenchmark`.

**Secrets:** `secrets.properties` lives at `android-app/secrets.properties` (next to `settings.gradle.kts`), NOT the repo root. See `android-app/secrets.properties.example`.

**Relay SDK from GitHub Packages + dependency verification (M4 Step 2, PR #18 — fully CI-validated).** The Secure Gateway SDK (`com.contextsolutions.securegateway:{core,java,android}`) is consumed from secure-gateway's **private GitHub Packages** Maven registry (NOT mavenLocal — that path is gone). Every build that resolves it (any `:shared`/`:androidApp`/`:desktopApp` build) needs **`read:packages`** auth: set `gpr.user`/`gpr.key` in `~/.gradle/gradle.properties` (a classic PAT with `repo` + `read:packages`) or env `GITHUB_ACTOR`/`GITHUB_TOKEN`. CI passes the **`SECURE_GATEWAY_PAT` repo secret** (cross-repo, so the built-in token can't read it; **fine-grained PATs don't reliably read the Maven registry — use a classic PAT**). **Gradle dependency verification is ON** (`android-app/gradle/verification-metadata.xml`, sha256, `verify-metadata=true`, ~1395 components — every dependency pinned).

**Regenerate after any dependency bump** (the metadata is the canonical source of which artifacts each build path resolves — generation taught us each path resolves a different set):
```bash
# Cold cache so the GitHub-Packages SDK bytes (not a local build) are pinned + every parent/BOM POM is fetched:
rm -rf ~/.gradle/caches/modules-2
export GITHUB_ACTOR=<you> GITHUB_TOKEN=<PAT-with-read:packages>
# Main paths (CI: android unit + desktop package; + dependency/buildscript graphs):
./gradlew --write-verification-metadata sha256 \
  :androidApp:testDebugUnitTest :shared:desktopTest :desktopApp:packageDistributionForCurrentOS \
  :androidApp:dependencies :shared:dependencies :ui:dependencies :desktopApp:dependencies :desktopHarness:dependencies buildEnvironment
# Lint/release path (installRelease/installDebug + lint resolve the Lint tool on a detached config the above miss).
# MUST use --no-configuration-cache — the lint detached config can't be config-cache-serialized (else "could not be cached"):
./gradlew --write-verification-metadata sha256 --no-configuration-cache \
  :shared:extractAndroidMainAnnotations :androidApp:lintAnalyzeRelease :androidApp:assembleDebug :androidApp:lintAnalyzeDebug
```
`--write-verification-metadata` **merges** (adds, never prunes), so hand-added entries survive. **Per-OS desktop natives** (Skiko/Compose `macos-*`/`windows-x64`) can only resolve on that OS — the macOS/Windows variants are **hand-added** (SHA-256 is OS-independent, fetched from Maven Central); a desktop dep bump that adds a new native fails the macOS/Windows `desktop-package.yml` jobs, which name the missing artifact to add. **Validation oracle:** `desktop-package.yml` runs only on `workflow_dispatch`/`v*` tags — `gh workflow run desktop-package.yml --ref <branch>` to validate all 3 OSes before merge.

## Working norms

- **Don't commit unless explicitly asked.** The user reviews diffs and decides.
- **Read the official integration guide BEFORE introspecting the JAR.** `https://ai.google.dev/edge/litert-lm/android` documents intended usage that can't be reverse-engineered from signatures. Then `javap -cp <jar> -p com.google.ai.edge.litertlm.X` for exact signatures.
- **Keep all LiteRT-LM types behind `LiteRtInferenceEngine`.** The `InferenceEngine` interface in `commonMain` is the seam.
- **Documentation hygiene:** when a Phase 1 decision changes, update both `PHASE1_PLAN.md` and `M0_DECISION_MEMO.md`.
- **Brief responses.** The user reads diffs and code; don't recap them in prose.
